# Progress Log

## 2026-09-13
- Scaffolded `server/` (Express 5 + TypeScript + MongoDB/Mongoose, MVC folders: models/controllers/routes/middleware/utils). Auth (register/login + JWT), Product CRUD, Order endpoints.
- Scaffolded `frontend/` (Next.js 16 App Router + TypeScript + Tailwind). Added axios client, zustand auth store, example login page.
- Connected frontend <-> backend (CORS verified) and backend <-> MongoDB Atlas.

## 2026-09-14
- Added `docs/` folder to track architecture decisions and progress (this file + `ARCHITECTURE.md`).
- Restructured frontend into explicit MVC layers: `models/`, `services/`, `controllers/`, `views/`, `components/ui/`.
- Built site-wide Navbar: logo, nav links, language switch (English/Bangla — UI only, no translation strings wired yet), phone number + social links, cart button (opens modal, not a page), auth area (Sign in button vs. profile dropdown).
- Built AuthModal: email/password login + signup (react-hook-form + zod, hits the existing Express API) plus Google/Facebook OAuth buttons via NextAuth.js v5 (GitHub removed 2026-09-14 per user preference).
- Built CartModal (zustand cart store, client-only for now — no backend cart persistence yet).
- Built ProfileDropdown (Dashboard/Orders/Settings/Logout placeholders) and placeholder pages for Dashboard/Orders/Settings.

## 2026-09-14 (later)
- Converted `Modal` primitive into a right-side sliding drawer (mount/unmount tied to the transition end, no library); `AuthModal` and `CartModal` now use it.
- New color palette applied via CSS variables in `globals.css` (Tailwind v4 `@theme inline`): muted sage/teal `primary`, warm cream `background`/`surface`, soft charcoal `foreground`, with a dark-mode variant. All views now use theme tokens (`bg-primary`, `text-muted`, `border-border`, etc.) instead of hardcoded black/white/neutral classes.
- Switched font from Geist to Inter (plus JetBrains Mono for `--font-mono`) and normalized text weights (`font-normal`/`font-semibold`) — "more normal" typography.
- Language switcher moved into the navbar's top strip, next to the phone number/social links.
- Navbar is `sticky top-0`; background is transparent at the top of the page and gains a blurred surface + shadow once scrolled (`isScrolled` state via a scroll listener).
- Split routing into two route groups: `app/(public)/layout.tsx` (Navbar + AuthModal + CartModal — Home/Shop/Categories/About/Contact) and `app/(protected)/layout.tsx` (sidebar nav — Dashboard/Orders/Settings). Root `app/layout.tsx` is now just the html/body shell + fonts + `Providers` + `Toaster`.
- Added `controllers/useRequireAuth.ts`: client-side guard that redirects unauthenticated users away from `(protected)` routes. Note: this is a client-only guard (no server-side session check yet), so there's a brief "Checking your session..." flash rather than a true SSR redirect.

## 2026-09-15
- Navbar reworked: `fixed` instead of `sticky` (so pages control their own top offset via a measured `--navbar-height` CSS var), permanent light glassmorphism panel with a distinct `nav-surface` tone from the page background (previously nav and page were both near-white and hard to tell apart), always-legible dark text (dropped the transparent/white-text-over-hero-photo treatment — unreliable contrast, see ARCHITECTURE.md).
- Hero height changed to `calc(100vh - navbar height)` so the homepage fills the viewport instead of leaving a blank gap below the fold.
- Dark mode is now opt-in only (gated behind `[data-theme="dark"]`, no toggle exists yet) instead of following the OS's `prefers-color-scheme` — it was silently overriding the light/cream palette for anyone with system dark mode on.
- **OAuth is now wired end-to-end**: `server/src/models/User.ts` supports OAuth-only accounts (`provider`, `providerId`, `image`, optional `password`); added `POST /api/auth/oauth-sync` (protected by a shared `INTERNAL_API_SECRET` header so only the Next.js server can call it) that finds-or-creates a user and issues a real backend JWT. `frontend/src/lib/auth.ts`'s NextAuth `jwt` callback calls it server-side; `controllers/useOAuthBridge.ts` feeds the result into the existing `useAuthStore`, so OAuth and email/password users are indistinguishable everywhere else in the app. `useAuthController` simplified accordingly. Verified with `oauth-sync` called directly (rejected without the secret, creates then updates-not-duplicates a user with it).
- Rotated `JWT_SECRET` in `server/.env` off its placeholder value to a real random secret.

### Known follow-ups
- Google/Facebook OAuth still need real app credentials (client id+secret) in `frontend/.env.local` — the sync plumbing works, but sign-in itself will fail until those are set.
- Language switcher only stores the selected locale; no message catalog/i18n library wired up yet.
- Cart is in-memory only (zustand, no persistence to backend or across auth).
- `useRequireAuth` is a client-only guard (no server-side session check), so protected pages briefly show "Checking your session..." before redirecting.

## 2026-09-14 (RBAC)
- Added a permission-based role system so the site owner can create limited-access "co-admins" instead of every admin being a full superuser. See `docs/ARCHITECTURE.md`'s new "Roles & permissions" section for the full design.
- `User.role` is now `"user" | "coadmin" | "admin"`; added `User.permissions: Permission[]` (`"products:manage" | "orders:manage"` for now, defined in `server/src/utils/permissions.ts`).
- New `middleware/auth.ts#authorize(permission)` (admin bypass + DB-fresh permission check) replaces the blanket `adminOnly` on product/order write routes. `adminOnly` itself is now DB-backed too (was trusting the JWT's `role` claim) and reserved for user/role management only — not delegable, to prevent privilege escalation.
- New `GET /api/users` / `PATCH /api/users/:id/access` (admin-only) let an admin list users and set another user's role + permissions; a user can't change their own access (self-lockout guard).
- Frontend: `views/AdminUsersView.tsx` (table with per-row role select + permission checkboxes) at `/admin/users`, guarded by `controllers/useRequireAdmin.ts`, linked from `DashboardSidebar` for admins only. `useAuthController` gained `hasPermission()` for UI-level gating elsewhere.
- Added `npm run promote-admin -- <email>` (`server/src/scripts/promoteAdmin.ts`) to bootstrap the very first admin, since no account can grant admin access through the app until one exists.

- Hardened to a strict single-admin invariant: `PATCH /api/users/:id/access` can only assign `"user"`/`"coadmin"` (400s on `"admin"`), the frontend role dropdown never offers "Admin" for anyone but the (fully disabled) current-user row, and `promote-admin` now *transfers* the role — it demotes any existing admin in the same run instead of creating a second one.
- `DashboardSidebar` now builds its admin section dynamically instead of a fixed list: "Manage Products" / "Manage Orders" each show up only if `hasPermission("products:manage")` / `hasPermission("orders:manage")`, "Manage Users" only for the admin — same permissions that gate the underlying API routes, so a coadmin's sidebar only ever shows what they can use. Added matching placeholder pages `/admin/products` and `/admin/orders` (mirroring the existing `/dashboard`/`/orders`/`/settings` placeholders), each guarded by the new `controllers/useRequirePermission.ts` (same pattern as `useRequireAdmin`, but keyed to one permission instead of the admin role).

## 2026-09-14 (Manage Products)
- `/admin/products` (`views/AdminProductsView.tsx`) is now a real product management screen instead of a placeholder: a table of all products (thumbnail, category, price, stock) with add/edit/delete, still gated by `useRequirePermission("products:manage")`.
- `views/ProductFormModal.tsx` — a drawer form (react-hook-form + zod, matching `AuthModal`'s conventions) for name/description/price/stock/category plus image upload. Category is a plain text input with a `<datalist>` of categories already in use (there's no separate Category collection — `Product.category` has always been free text, see `server/src/models/Product.ts`). The form's inner fields live in a `ProductForm` subcomponent mounted fresh (via `key={editingProduct?._id ?? "new"}`) each time the drawer opens, instead of a `useEffect` that resets state — simpler, and sidesteps the new `react-hooks/set-state-in-effect` lint rule that ships with Next 16's `eslint-config-next`.
- Backend: `PUT /api/products/:id` now also accepts `multipart/form-data` (added `upload.array("images", 5)`, previously only `POST` did). `updateProduct` merges kept existing images (sent as a JSON-stringified `existingImages` field, since removing a specific image had no prior mechanism) with any newly uploaded files.
- `services/productService.ts` (list/get/create/update/delete) and `controllers/useAdminProducts.ts` (list state) added, following the same service/controller split as `userService.ts`/`useAdminUsers.ts`.
- Added `lib/api.ts#toUploadUrl` — uploaded images are served from the backend's origin at `/uploads/...`, not under `api`'s `/api`-prefixed baseURL, so this strips that prefix to build a displayable `<img>` src.
- **Fixed a pre-existing bug from the RBAC work**: `useAdminUsers`'s original `useEffect(() => { load() }, [load])` fetch-on-mount pattern tripped `react-hooks/set-state-in-effect` (a new lint rule bundled with this project's Next.js/eslint-config-next version) — it just hadn't been linted after being added. Both `useAdminUsers` and the new `useAdminProducts` now fetch via a `.then()/.catch()/.finally()` chain with an `ignore`-flag cleanup instead of a named async callback invoked directly in the effect body — genuinely defers the state updates instead of running them synchronously inside the effect, which is what the rule is actually checking for.
- Investigated a report of "only one image saves" — found no bug in the actual upload path (the file input has `multiple`, every file is appended under the same `images` form field, and the server's `upload.array("images", 5)` accepts up to 5). The real gap: `AdminProductsView`'s table only ever rendered `product.images[0]`, so even a product saved with 5 images only ever showed one thumbnail. Now shows an explicit "N images" count next to the thumbnail so this is verifiable at a glance. Also: there was nowhere to see products at all outside the admin table — `/shop` and `/categories` were still `PlaceholderPage`s.
- `/shop` and `/categories` are now real pages instead of placeholders — both async Server Components that call `productService.getProducts()` directly (per the "server-rendered data vs. client state" rule in `ARCHITECTURE.md`, no client fetch needed since this is public, non-personalized data) and pass the result to a new `views/ProductGrid.tsx` / `views/ProductCard.tsx`. `/shop` accepts a `?category=` query param (Next 16 passes `searchParams` as a `Promise`, per its docs) that both filters the grid server-side and is what `/categories`' category cards link to.

### Known follow-ups (new)
- No individual product detail page yet (`/shop/[id]`) — `ProductCard` is not a link to anywhere. Worth adding once there's a reason to (e.g. "view details", add-to-cart from a detail page).
- Category matching on `/shop?category=` is an exact, case-sensitive string match against `Product.category` (there's still no separate Category collection) — two products entered as "Shoes" and "shoes" would show as different categories.

## 2026-09-14 (Dynamic banners)
- The homepage hero slider (`HeroSlider`/`useHeroSlider`, unchanged) is no longer fed hardcoded mock data — `server/src/models/Banner.ts` is a real collection, and `/admin/banners` (`views/AdminBannersView.tsx`, gated by the new `banners:manage` permission) is a dashboard screen to add/edit/delete/reorder/publish slides, mirroring the Products admin screen's structure.
- Backend: `GET /api/banners` (public) returns only `isActive` banners sorted by `order`, for the landing page. `GET /api/banners/all` (needs `banners:manage`) returns every banner, active or not, for the admin table. `POST`/`PUT` accept `multipart/form-data` (single `image` file via `upload.single`, plus `primaryCta`/`secondaryCta` as JSON-stringified fields, parsed server-side). `PATCH /:id/move` does reordering server-side (swaps `order` between a banner and its up/down neighbor in one request) rather than have the client juggle two racy PUTs.
- `services/homeService.ts` (the mock) is gone — `app/(public)/page.tsx` now calls `services/bannerService.ts#getPublicBanners()` directly, which fails soft (empty array, not a thrown error) if the backend is unreachable, so a backend hiccup doesn't take the homepage down. An empty/all-hidden banner set falls back to `HeroSlider`'s existing pulsing-placeholder state — no separate empty-state work needed.
- Added `banners:manage` to the permission list (`server/src/utils/permissions.ts` / `frontend/src/models/index.ts`'s `ALL_PERMISSIONS`) and a "Manage Banners" sidebar link gated the same way as Products/Orders.
- `BannerFormModal` uses the same "fresh-mount-via-key" pattern as `ProductFormModal` (avoids `react-hooks/set-state-in-effect`, see `ARCHITECTURE.md`) and `useWatch` (not `watch()`) to read the "add a second button" checkbox reactively — `watch()` itself trips a React Compiler warning about being unmemoizable.

### Known follow-ups (new)
- No image-per-banner size/aspect validation — an oddly-cropped upload will just look bad in the slider, nothing rejects it.
- Reordering is only up/down one step at a time (no drag-and-drop) — fine for a handful of banners, would want revisiting if the list grows long.

### Fixed: banner images broken (top-left broken-image icon) — two layered causes
1. `getPublicBanners()` returned `image` straight from the API as a bare `/uploads/...` path, but `HeroSlider` passes it directly to `next/image`, which resolves a path like that against the *frontend's* own origin (`localhost:3000`) — the backend (`localhost:5000`) is what actually serves `/uploads`. Fixed by applying the existing `toUploadUrl` helper when banners are mapped to the public `HeroSlide` shape (same helper already used everywhere else images render).
2. Next 16's image optimizer added an SSRF guard that refuses to fetch any upstream hostname resolving to a private/loopback IP (`400 upstream image ... hostname resolved to private IP`) — `localhost:5000` trips this by default now. Since that's just our own backend in dev, added `images.dangerouslyAllowLocalIP: true` to `next.config.ts`. **Note:** `next.config.ts` changes require a full dev server restart — Next does not hot-reload it.

## 2026-09-14 (Product detail page, reviews, pagination, nav active-state, SEO pass)
- **Navbar active-route highlighting**: `Navbar.tsx` now uses `usePathname()` to mark the current section's link (`text-primary font-medium` + `aria-current="page"`), both in the desktop `<ul>` and the mobile menu. A link is active on its own route and anything nested under it (`/shop` also covers `/shop/[id]`) except Home, which only matches `/` exactly. Deliberately did *not* try to also highlight "Categories" when on `/shop?category=` — that would need `useSearchParams()` in a component rendered on every public page, which risks opting the whole public route group out of static rendering; scoped to path-based matching only.
- **Product detail page** (`/shop/[id]`): clicking a product in `/shop` now goes to a real page instead of nowhere — `ProductCard` is a `<Link>`. Two-part layout: the main column (`ProductGallery` — click-to-switch thumbnails, `ProductBuyBox` — availability text, quantity stepper, Add to Cart / Buy Now, `ProductReviews` beneath) and a `ProductSuggestions` sidebar (same-category products, current one excluded, empty-state renders nothing). "Buy Now" adds to cart and opens the existing cart drawer; "Add to Cart" just adds. Both reuse the existing `useCartStore`/`useUIStore` — no new cart logic.
- **Reviews**: new `Review` model (`server/src/models/Review.ts`, one per user per product via a unique compound index — resubmitting updates the existing review rather than duplicating) and `GET`/`POST /api/products/:id/reviews`. Average rating + count come back alongside the paginated list so the UI doesn't need a second request. `ProductReviews` shows a star-rating summary, the list, a submission form for signed-in users (a "Sign in to leave a review" prompt otherwise, wired to the existing `AuthModal` via `useUIStore.openAuthModal`), and its own pagination.
- **Pagination**: `GET /api/products` now returns `{ items, total, page, totalPages }` instead of a bare array (capped at 48/page). New reusable `views/Pagination.tsx` supports both `hrefFor` (Link-based, for server-rendered pages like `/shop`) and `onChange` (button-based, for client-only lists like the admin products table and the reviews list) — same component either way. Applied to: `/shop`, the admin Manage Products table, and the reviews list on a product page.
- **New `GET /api/products/categories`** (Mongo `$group` aggregate) backs `/categories` directly with `{category, count}` pairs instead of fetching every product client-side just to count them.
- **SEO pass** (the gap called out: banner/shop/category pages weren't fully covered): `/shop` now uses `generateMetadata` for a category-aware title ("Nike — Shop" when filtered) instead of a static title; the new product page has per-product `generateMetadata` (title/description/OG image derived from the product) plus `Product` JSON-LD (price, availability, aggregate rating when reviews exist); `app/sitemap.ts` is now `async` and paginates through every product to list `/shop/[id]` for each one, alongside the previously-static route list (which never included `/shop`/`/categories` at all — that gap is also fixed here).

### Known follow-ups (new)
- Reviews have no moderation/reporting and no "verified purchase" concept — any signed-in user can review any product regardless of whether they bought it.
- `/categories` itself isn't paginated (categories aren't expected to number in the hundreds); revisit if that assumption stops holding.
- `ItemList` structured data still isn't on `/shop`/`/categories` (only the per-product page has structured data) — the product page now has both `Product` and `BreadcrumbList` JSON-LD, see below.

## 2026-09-14 (Per-product delivery fee, two tiers + currency)
- `Product.deliveryFeeInsideCity` / `deliveryFeeOutsideCity` (both `number`, default 0) — two flat fees the admin types in manually, same as price/stock, not calculated from any courier's rates (there's no shipping-provider integration, and given the size of the store that's a reasonable call for now, not a gap to fix later unless that changes). Started as a single `deliveryFee` field, split into two the same day once the store owner clarified inside-city vs. outside-city delivery costs differently. Existing products transparently read back with both at `0` via Mongoose's schema defaults — no migration needed either time.
- Shown everywhere a product's price shows: `ProductCard` ("From ৳X delivery", using whichever tier is cheaper, or "Free delivery" if both are 0), the product detail page's `ProductBuyBox` (both tiers spelled out: "৳X inside city · ৳Y outside city", with a truck icon), and the admin Manage Products table (a combined "Delivery (in/out)" column). Editable in `ProductFormModal` as two side-by-side inputs right next to Price/Stock.
- The product page's `Product` JSON-LD `offers.shippingDetails` uses the inside-city fee as the representative rate — schema.org can express genuinely per-region shipping via `shippingDestination`/`DefinedRegion`, but "inside vs. outside city" isn't a defined region we have real geographic data for, so that would mean fabricating one rather than using it correctly.
- **Currency switched from USD to Bangladeshi Taka** (the store owner is in Bangladesh): new `lib/currency.ts` (`formatCurrency`, `৳` symbol, `BDT` code) replaces every hardcoded `$`/`.toFixed(2)` across `ProductCard`, `ProductSuggestions`, `ProductBuyBox`, `CartModal`, `AdminProductsView`, and the product detail page, plus `priceCurrency`/shipping `currency` in the JSON-LD (`USD` → `BDT`). One helper, one place to change if this ever needs to change again.
- **Still not done (unchanged from last time, still out of scope for this pass)**: the cart/checkout total doesn't sum delivery fees at all yet — `useCartStore.totalPrice()` and `CartModal`'s subtotal are still just `price × quantity`, and there's no cart-level "how am I shipping this" (inside vs. outside city) selection to even pick which tier applies.

## 2026-09-14 (Promo codes — manual entry, shareable links, and banner-driven discounts, unified)
Three asks in one request turned out to be the same underlying feature: a promo code system where a code can be applied either by typing it in, or automatically from a URL — which covers "share a link that gives a discount" and "a banner button triggers a discount" for free, since **banners already support arbitrary link URLs** (`primaryCta.href`/`secondaryCta.href`, no schema change needed there at all). Pointing a banner's button at `/shop?promo=CODE` (store-wide) or `/shop/<productId>?promo=CODE` (one product) is the entire "banner-driven cost cut" feature.

- **Backend**: new `PromoCode` model (`code`, `discountType: "percentage" | "flat"`, `value`, `scope: "all" | "product"` + optional `product` ref, `isActive`, optional `expiresAt`). Admin CRUD at `/api/promo-codes` (needs the new `promotions:manage` permission — added to the delegable permission list alongside products/orders/banners). `POST /api/promo-codes/validate` is **public** (no auth) — both the cart's manual entry box and a code auto-applied from a link need to work for guests. It only confirms a code's terms (type/value/scope); the actual discount math happens client-side against the cart, same as the rest of the cart (still no backend order/checkout to check it against — see Known follow-ups).
- **Cart** (`useCartStore.ts`): added `promo: AppliedPromo | null`, `applyPromo`/`clearPromo`, and `discountAmount()`/`grandTotal()` alongside the existing `totalPrice()` (now explicitly the pre-discount subtotal). A `scope: "product"` promo only discounts that one line item, and only if it's actually in the cart (a shared product-specific link doesn't just discount whatever's in your cart). `CartModal` now has a promo code input (or, once applied, an "applied" chip with a Remove button), a Subtotal/Discount/Total breakdown.
- **Auto-apply from a link**: new invisible `views/PromoAutoApply.tsx`, mounted on `/shop` and `/shop/[id]` (both already read `searchParams` server-side, so passing `promo` through costs nothing extra — deliberately did *not* reach for `useSearchParams()` client-side here, which would've opted the whole public route into needing a Suspense boundary). A bad/expired code in a shared link fails silently rather than blocking the page.
- **Admin**: `/admin/promotions` (`views/AdminPromotionsView.tsx` + `PromoCodeFormModal.tsx`, gated by `promotions:manage` like Products/Orders/Banners) — create/edit/delete codes, see each one's status (Active/Inactive/Expired), and a one-click "copy share link" button per code that builds the right `?promo=` URL depending on its scope.

### Known follow-ups (new)
- **No usage-limit enforcement** (`maxUses`/`usedCount`) — deliberately left out rather than half-built, since there's no order/checkout-completion event yet to hook an increment into; adding a usage cap now would either not decrement anywhere or decrement on every re-validation (wrong either way).
- A `scope: "product"` code's JSON-LD/structured-data implications weren't touched — that's a separate, smaller follow-up if it matters for SEO on a discounted product page.
- The product picker in the promo code form only lists the first 48 products (one page) — fine at the store's current size, would need real search-as-you-type if the catalog grows past that.

## 2026-09-14 (On-site promo discoverability)
Follow-up to the promo code system above: codes only reached customers via a link or by already knowing them — nothing on the site itself surfaced an active promo. Added two public-facing pieces, both fed by a new `GET /api/promo-codes/active` (public, minimal shape — no id/timestamps, just what a customer needs) that returns every currently active, non-expired code:

- **Sitewide announcement strip** (`views/PromoAnnouncementBar.tsx`) for a `scope: "all"` code — rendered *inside* `Navbar`'s own fixed header (not as a separate element above it), so it's automatically included in the `--navbar-height` measurement and scrolls with the rest of the header instead of getting overlapped. `app/(public)/layout.tsx` is now `async`, fetches the active list once, and passes the first sitewide one to `<Navbar sitewidePromo={...} />`. Its link (`/shop?promo=CODE`) reuses the exact same auto-apply mechanism as a shared product link.
- **Per-product badges** for a `scope: "product"` code: `ProductCard` (shop grid, via a `promoByProductId` map the Shop page builds from the active list) and the product detail page's `ProductBuyBox` (which also gets a one-click **Apply** button — no need to even open the cart or retype the code; it calls `applyPromo` directly and reflects "Applied" once it matches the cart's current promo). A product page falls back to showing a sitewide code if there's no product-specific one, since either would discount that item.
- New shared `lib/promo.ts#formatPromoDiscount` (`"10% off"` / `"৳50 off"`) used by both the announcement bar and the badges, so the wording stays consistent.
- Verified against the running app with a real promo code already in the database (`NOWAY10`, product-scoped) — confirmed the badge renders on both the shop grid and that exact product's page.

## 2026-09-14 (Banners now link to the promo they promote)
Follow-up: a banner announcing a sale had no actual connection to a promo code — the admin had to hand-type the discount into the banner's text and hand-type `?promo=CODE` into its button link, with nothing keeping them in sync. `Banner` now has an optional `promoCode` ref (`server/src/models/Banner.ts`).

- `GET /api/banners` (public) populates it and includes live `{code, discountType, value, scope, productId}` in the response — but only when that code is still actually active/unexpired, so a banner whose linked code got deactivated quietly falls back to a plain CTA instead of advertising a dead discount.
- `HeroSlider` shows the discount as a small badge on the slide when present, and both CTA buttons' links now go through `lib/promo.ts#withPromoParam`, which appends `?promo=CODE` automatically — so the link always applies the code even if the admin forgot to add it by hand (or the code changes later).
- Admin: `BannerFormModal` gained a "Linked promo code" dropdown (`useAdminBanners` now also fetches the promo list, failing soft if the coadmin has `banners:manage` but not `promotions:manage`); `AdminBannersView`'s list shows a small tag with the linked code per banner.

## 2026-09-14 (Fixed: coadmin's granted access wasn't showing up)
Real bug, not just a caching nuance: `register`/`login`/`oauthSync` in `authController.ts` never included `permissions` in the response `user` object at all — only `id`/`name`/`email`/`role`. So a coadmin's `permissions` was `undefined` from the moment they logged in, regardless of what was actually granted in the database, and every `hasPermission()` check on the frontend (which is what `DashboardSidebar` uses to decide which admin links to show) was silently always false. This is why granting a coadmin `products:manage` never made "Manage Products" appear — it was never sent to their browser in the first place, not a staleness issue.

- Fixed by adding `permissions` (and `image`, missing from `register`/`login` too) to all three response shapes.
- Also added `GET /api/auth/me` (protected) and a `useRefreshUser` hook (mounted once in `app/providers.tsx`, alongside the existing OAuth bridge) that refreshes the cached `user` on every app load. This covers the *other*, real remaining gap: an admin changing a coadmin's access **after** that coadmin already has a browser session open — the API already enforces the change immediately (it re-checks the DB on every request, see `middleware/auth.ts`), but the cached frontend `user` used for sidebar visibility didn't update until a fresh login. Now a normal page refresh is enough, not a full log-out/log-in.

## 2026-09-14 (Banner promo auto-detection + customer dashboard)
- `getBanners` (public) now shows a banner's discount two ways: an explicitly linked promo code (existing), or — with zero extra setup — one **inferred** from a CTA that points straight at a promoted product's page (`/shop/<productId>`). Either way only counts if that code is still active/unexpired. This matched what the store owner actually expected ("the product has a promo, the banner promotes that product, why doesn't it show") without needing them to separately link the same code twice.
- `/dashboard` (was a bare placeholder for every logged-in user) is now `views/DashboardOverview.tsx`: total order count, account email/role, the 5 most recent orders (status badge, item count, total) via the already-existing `GET /api/orders/my`, and quick links to Orders/Settings. New `services/orderService.ts` + `controllers/useMyOrders.ts` + `Order`/`OrderStatus` types follow the same service/controller pattern as everything else.
- **Deliberately scoped to the customer view only** — an admin/coadmin overview (pending orders, low stock, active promos at a glance) was discussed and intentionally deferred: it needs new aggregate-stats endpoints, and would look thin anyway before checkout is real and orders actually exist. `/dashboard` currently shows the same personal-orders view to every role, admins included.

### Known follow-ups (new)
- No admin/coadmin dashboard overview yet (see above) — a real next step once there's checkout-driven order volume to summarize.
- `useMyOrders` has no pagination — fine while a customer's order count is small, would want it if that stops being true.

## 2026-09-14 (Settings and Orders finished)
- **Settings** (`views/SettingsView.tsx`, was a placeholder): a Profile section (name + avatar upload, email shown read-only) and a Password section — the latter only rendered for `provider === "local"` accounts, since a Google/Facebook account has no password to change (shows an explanatory note instead). Backend: `PATCH /api/auth/profile` (name + optional image, reuses the same `utils/upload.ts` multer config as products/banners) and `PATCH /api/auth/password` (requires the current password via `comparePassword`, rejects non-local accounts with a clear message). Saving a profile change calls `useAuthStore.updateUser` so the navbar/dropdown avatar updates immediately, no refresh needed.
- Added `provider` to `User`/`ManagedUser`'s shared response shape (`authController.ts`'s new `shapeUser` helper, replacing five copies of the same object literal across register/login/getMe/updateProfile/oauthSync) — the frontend needed it to know whether to show the password section at all.
- **Orders** (`views/OrdersView.tsx`, was a placeholder): every order on the account, each showing its line items with product thumbnail/name/qty/price (not just a count like the dashboard's recent-5 widget), status badge, total, and a one-line shipping summary. Needed `GET /api/orders/my` to actually populate `items.product` (previously just an unresolved id) — added `.populate("items.product", "name images")`; a deleted product now resolves to `null` there and the UI shows "Product no longer available" rather than crashing.
- Already-logged-in sessions (from before `provider` was added to the response) pick this up automatically via the existing `useRefreshUser` hook on next page load — no forced re-login needed, same mechanism that fixed the coadmin permissions bug.

## 2026-09-14 (Delivery location: Settings → Dashboard, server-backed)
- `User.deliveryLocation?: { address, city, isInsideCity }` (`server/src/models/User.ts`) — a saved default location, separate from `Order.shippingAddress` (which is its own per-order snapshot once checkout is real). `isInsideCity` is manual, same idea as `Product.deliveryFeeInsideCity`/`OutsideCity` — no real courier/zone lookup, the customer just says which applies to them.
- `PATCH /api/auth/delivery-location` (protected) saves it; it's included in `shapeUser`, so it comes back on login/`/me`/profile-update like everything else about the account.
- Settings gained a "Delivery location" section (address, city, inside/outside-city radio) between Profile and Password, saved via `authService.updateDeliveryLocation` → `useAuthStore.updateUser`.
- Dashboard shows it in a card right above Recent Orders — the saved address/city/inside-or-outside badge, or an "add one" prompt linking to Settings if nothing's saved yet.

## 2026-09-14 (Admin analytics)
New `analytics:manage` permission (delegable to coadmins, same pattern as products/orders/banners/promotions) backs `/admin/analytics`.

- **New tracking**: `AnalyticsEvent` model (`page_view` | `product_click`, a path, optional product ref, optional user ref, and a `sessionId` — a random id the browser generates once into `localStorage` so anonymous/guest traffic can still be counted as "unique visitors," not just logged-in users). `POST /api/analytics/track` is public (guests must be able to fire it) but uses a new `optionalAuth` middleware — attaches the user if a valid token happens to be present, never rejects the request either way.
- **What fires it**: `controllers/usePageViewTracking.ts` (mounted once, globally, in `app/providers.tsx` alongside the OAuth bridge) fires on every route change via `usePathname()` — except `/admin/*`, deliberately excluded so an admin browsing their own control panel doesn't pollute the traffic numbers *that same panel* reports. `views/ProductClickTracker.tsx` (invisible, mounted on `/shop/[id]`) fires a `product_click` on arrival — tracking the product page view itself rather than intercepting every card's `onClick` catches every entry point (grid, suggestions, a shared link) with one component. Both are fire-and-forget (`lib/analytics.ts`) and never block or fail the page they're on.
- **`GET /api/analytics/overview`** (needs `analytics:manage`) returns one consolidated payload — products (total, total stock, low-stock count, by category), users (total, by role, by sign-in method), orders (total, by status, revenue from paid/shipped/delivered), traffic (total page views, unique visitors, top 10 routes), and product clicks (total, top 10 products by view count, names resolved server-side). One endpoint rather than several, since the dashboard always wants all of it at once.
- `views/AdminAnalyticsView.tsx`: a KPI row (8 stat tiles) plus six sections, each a small horizontal bar list (loaded the `dataviz` skill for this — single sequential hue via the app's existing `primary` token throughout, since each row's identity is carried by its label rather than by color; width proportional to the section's largest value; every value also shown as a direct label).

### Known follow-ups (new)
- No time-range filtering (last 7/30 days, etc.) — everything is all-time totals. Worth adding once there's enough event volume for a single all-time number to stop being the useful view.
- `AnalyticsEvent` has no retention/cleanup — every page view and click is kept forever. Fine at current scale, would want a TTL index or periodic archival if traffic grows.

## 2026-09-14 (Homepage redesign)
Added, in order below the hero: **Shop by Category** (up to 6 category cards, `getProductCategories()`, links to `/shop?category=X`), **Featured Products / New Arrivals** (see below), **trust badges** (`views/TrustBadges.tsx` — Fast Delivery/Secure Shopping/Easy Returns/Dedicated Support, static value-prop row, standard ecommerce-homepage content), and a 5-question **FAQ** (`views/FAQSection.tsx`, native `<details>`/`<summary>` accordion — no JS library needed — plus `FAQPage` JSON-LD for rich-result eligibility).

- **Product highlight, done as real admin control rather than a heuristic**: `Product.isFeatured: boolean` (admin-set, a checkbox in `ProductFormModal` — "Feature on homepage", shown with a star in the admin table) and `GET /api/products?featured=true`. The homepage fetches featured products first; if none are marked yet (a fresh store), it falls back to the latest products and the section relabels itself "New Arrivals" instead of showing empty — `FeaturedProducts` takes an `isFallback` flag for this.
- Loaded the `dataviz` skill's stat-tile/KPI guidance isn't relevant here (no charts on this page), but its general "form before color" discipline shaped the trust-badges row (icon + label + one-line description, no decorative color beyond the existing `primary` token).
- All copy referencing the delivery zone reuses the `STORE_CITY` constant (`lib/seo.ts`) already introduced for Settings/Dashboard, so "Sylhet" only needs to change in one place.

### Known follow-ups (new)
- No products are marked featured yet in the live data — the homepage is currently showing the "New Arrivals" fallback, not true admin-curated picks. Mark a few products as featured to see the intended section.

## 2026-09-14 (Policy pages, real Contact page, centralized contact info)
- **Discussed and decided against a custom chat system** — the site already has a `WhatsAppButton` mounted globally (`app/layout.tsx`), which covers real-time chat for a store this size without the cost of building/staffing a custom inbox. Revisit only if WhatsApp stops being enough or dedicated support staff get hired.
- **`/shipping`** and **`/returns`** — real policy pages (delivery zones/timelines tied to the existing inside/outside-{`STORE_CITY`} fee split, return window, refund process, exchanges), not placeholders. Linked from the homepage's "Fast Delivery"/"Easy Returns" trust badges, the footer, and added to `sitemap.ts`. "Secure Shopping" and "Dedicated Support" stayed as-is/linked to `/contact` respectively — deliberately not given their own pages, since there was no real policy content to put on them (a page that's just padding is worse than no page).
- **Contact page finished**: a real form (`views/ContactView.tsx`) alongside phone/email/WhatsApp/address cards. `POST /api/contact` (new `ContactMessage` model) **always saves the message to the database first** — SMTP isn't configured yet (`server/.env`'s `SMTP_*` fields are blank) — then best-effort emails a notification to `CONTACT_EMAIL` if that env var is set, matching the same non-blocking `.catch()` pattern as the existing welcome email.
- **Centralized contact info**: it was previously hardcoded in three separate places (`Navbar`'s phone, `Footer`'s phone/email/address, `WhatsAppButton`'s number) with placeholder values. New `lib/contact.ts` is now the one place to paste in the real phone/email/address/WhatsApp number — the user is providing these separately, so this is what makes that a one-file edit instead of a hunt-and-replace across the codebase.

### Known follow-ups (new)
- `lib/contact.ts` and `server/.env`'s `CONTACT_EMAIL` still hold placeholder values — waiting on the real contact details.
- No admin UI to read submitted `ContactMessage`s yet — they're saved and (if `CONTACT_EMAIL` is set) emailed, but there's no `/admin/messages` inbox. Worth adding if email notifications turn out to be an unreliable way to notice new messages.

## 2026-09-14 (Dashboard: wishlist, review prompts, reorder, promo reminder)
Four things discussed for the dashboard, all built except loyalty points (explicitly out of scope) and an account-security panel (explained what it is, recommended against it for a store this size).

- **Wishlist** — new subsystem: `User.wishlist: ObjectId[]` (ref `Product`), `GET/POST/DELETE /api/wishlist(/:productId)`. Frontend uses a **shared zustand store** (`useWishlistStore`, holding just the id `Set`) rather than a per-component hook — a shop grid renders many `ProductCard`s at once, and each independently fetching the wishlist would mean N requests instead of one. `useWishlistSync` (mounted globally in `providers.tsx`) loads it once there's a session and clears it on logout. `WishlistButton` (heart icon, optimistic toggle) is on both `ProductCard` (top-right of the image; `stopPropagation`/`preventDefault` since the card is itself a link) and the product detail page's `ProductBuyBox`. Dashboard's new "Saved items" section fetches full product details separately (the shared store only needs ids) and removing there also updates the shared store so the heart icon elsewhere reflects it immediately.
- **"Leave a review" prompts** — `GET /api/orders/reviewable` (new, in `orderController.ts`) finds every product across the user's *delivered* orders that doesn't already have a `Review` from them, deduped. Shown on the dashboard with a "Write a review" link to `/shop/<id>#reviews` — `ProductReviews`'s section now has `id="reviews"` + `scroll-mt-24` for that deep link to actually land in the right place under the fixed navbar.
- **Reorder** — a button per recent order that re-adds every line item back into the cart (`useCartStore.addItem`) and opens the cart drawer. Skips any item whose product was since deleted (`item.product === null`, from `getMyOrders`'s populate) rather than failing the whole action; if *every* item in the order is gone, shows an error instead of opening an empty result.
- **Active promo reminder** — same `getActivePromoCodes()` call the homepage banner and product badges already use, filtered to a sitewide (`scope: "all"`) code, shown as a dashboard banner linking to `/shop?promo=CODE`.
- **Account-security panel**: explained what one typically is (active sessions/devices, login history, connected sign-in methods, 2FA) but didn't build it — real value at scale, not proportionate for this store's size right now.

### Known follow-ups (new)
- Wishlist has no "move to cart" bulk action — only one-at-a-time via visiting each product page (Add to Cart is still per-product there).
- The reviewable-products list has no pagination — fine while a customer's delivered-and-unreviewed count is small.

## 2026-09-14 (Zila/Upazila location picker, and the order system finished)
The single biggest change so far — see `docs/ARCHITECTURE.md`'s new "Checkout & Orders" section for the full design. Two asks that turned out to be one build: a proper Bangladesh location picker, and an actual working checkout instead of a `createOrder` that had never been called from anywhere.

- **Location picker**: `User.deliveryLocation` and `Order.shippingAddress` both changed from a free-text `address`/`city`/manual-inside-city-toggle to `{zila, upazila, addressLine}` — Zila and Upazila are cascading selects from a real dataset (`frontend/src/lib/bangladeshGeo.ts`, 64 districts / 494 upazilas, fetched from `nuhil/bangladesh-geocode` on GitHub rather than typed from memory), only the final house/road/area detail stays free text. "Inside/outside the store's city" is no longer a separate manual question — it's derived by comparing the selected Zila to `STORE_CITY`, both in Settings and at checkout.
- **Checkout, actually built**: `/checkout` (protected, login required) — delivery details form (prefilled from `user.deliveryLocation`/`name`/new `user.phone`, all editable), a real order summary (items, delivery fee live-computed from the selected Zila, any applied promo's discount, total), and "Place order" creates a real `Order` at `status: "pending"` (Cash on Delivery — there's no payment gateway integrated anywhere in this app). The server, not the client, computes every number and decrements stock — see ARCHITECTURE.md.
- **"Buy Now" and "Reorder" are now direct-order fast paths** — both add to cart and go straight to `/checkout`, skipping the cart drawer. The cart drawer's own "Proceed to checkout" button (previously dead) now actually links there too.
- **"Ask someone else to pay"**: a button on the Checkout page snapshots the cart into a `SharedCart` and returns a shareable `/pay/<id>` link. Anyone can open it and see what's being asked (no login needed to look); "Continue to checkout" loads those items into *their own* cart and sends them through the same checkout flow, logged in as themselves — so "paying" means completing the COD order themselves. The link shows "already paid for" once someone has.
- Added `User.phone` (Settings' Profile section, prefills checkout) since nothing captured a contact number anywhere before this.
- `Order` gained `itemsTotal`/`deliveryFee`/`promoCode`/`discount` as their own fields (not just a bare `totalAmount`), so `/orders` and the checkout confirmation can show a real itemized breakdown — Orders page updated to match.

### Known follow-ups (new)
- Still Cash-on-Delivery only — no real payment gateway (SSLCommerz/bKash/Nagad/Stripe) integrated. If one gets added, `createOrder`'s totals computation is the piece to hook it into.
- The checkout page uses the `(protected)` layout's dashboard sidebar — there's no distraction-free, sidebar-free checkout layout yet.
- No order-cancellation flow for the customer, and stock isn't restored if an admin cancels an order (`updateOrderStatus` just flips the status).
- `STORE_CITY` is duplicated as a constant in both `frontend/src/lib/seo.ts` and `server/src/utils/store.ts` (documented as "keep in sync" in both) rather than a single shared source — acceptable for a two-package repo without a shared types package, but a real seam if it ever drifts.

## 2026-09-14 (Footer, breadcrumb, suggestions fallback)
- New `views/Footer.tsx` (brand blurb, Shop/Company link columns, contact info, social icons, copyright) added to `app/(public)/layout.tsx` alongside `Navbar` — every public route gets it (not the `(protected)` dashboard/admin routes, which have their own sidebar chrome instead).
- Product detail page: added `views/Breadcrumbs.tsx` (Home / Shop / *Category* / Product name, category and Shop as real `<Link>`s) plus matching `BreadcrumbList` JSON-LD — the category is a real, crawlable internal link now (both in the breadcrumb and next to the product title), not inert text, which is what actually makes it count for SEO rather than just being decorative.
- Suggestions no longer silently disappear for a product whose category has nothing else in it: `/shop/[id]` now backfills from other products (any category, current one excluded) when the same-category match count is under 4, so "You might also like" reliably shows something.

### Fixed: real hydration mismatch (Navbar Sign-in/Menu icon) — unrelated to banners, pre-existing bug
`useAuthStore.isAuthenticated()` fell back to `Cookies.get("token")` when the store's own `token` was null. On the server, `document.cookie` doesn't exist so this always evaluated falsy (logged-out HTML). On the client, that cookie read *is* synchronously available on the very first render — before `Providers`' `hydrate()` effect even runs — so an already-logged-in user's first client render disagreed with the server-rendered HTML (`AuthArea` showing "Sign in" vs the profile dropdown), a genuine React hydration mismatch that just happened to get reported against the neighboring mobile-menu icon. Fixed by reading only the Zustand-managed `token` (matches server: null until `hydrate()` runs, then updates like any normal post-mount state change — no mismatch, just a very brief "Sign in" flash on hard refresh for a logged-in user, which is expected/correct for this pattern).

### Fixed: "Add banner" did nothing
`BannerFormModal`'s zod schema validated `secondaryCta.label`/`.href` as always-required (`ctaSchema.optional()` only allows the whole object to be `undefined`, not empty strings inside it) — but the form's default value for that field is always `{label: "", href: ""}`, never `undefined`, even when "Add a second button" is unchecked. So validation silently failed on every submit unless the user also filled in a secondary button they didn't want, and since no error message was rendered for those hidden fields, clicking "Add banner"/"Save changes" appeared to do nothing at all. Fixed by making `secondaryCta` unconstrained in the base schema and enforcing required-when-checked via `.superRefine`, plus now rendering an error message under those fields.

### Known follow-ups
- **No admin exists yet** — run `npm run promote-admin -- <your email>` in `server/` before any of this is usable.
- Role/permission changes only take effect on the *next request* for a currently logged-in coadmin (DB-backed checks), but their cached `user` object in the frontend store won't reflect a changed role until they log in again — fine for now since permissions gate the API, not just the UI, but worth a "force re-login" nudge later.

## 2026-09-14 (Login-in-place at checkout, payment method UI, shareable-link "Share", sidebar responsiveness, Manage Orders)

### Fixed: hitting a protected page while logged out silently redirected to "/"
`useRequireAuth` used to `router.replace("/")` the instant it saw no session — no explanation, and (for checkout specifically) it lost the point of being on that page at all. It now opens `AuthModal` in place instead; logging in just flips `isAuthenticated` and the page renders normally, no redirect. Only dismissing the modal without logging in sends you home. Hit a real React Strict Mode bug along the way: the effect's dev-mode double-invoke read a stale closed-over `isAuthModalOpen` on its second pass and fired the redirect before the modal's own "open" state had re-rendered through — fixed by reading `useUIStore.getState()` live instead of the subscribed value. `/checkout` specifically also now stays mounted (not gated at all) while logged out, since it's pure client-cart state with nothing that needs a fresh fetch after login — every other protected page (Dashboard/Orders/Settings/Admin) still fully gates, since their data hooks fetch once on mount and won't re-fire just because you logged in partway through.

### Payment methods — checkout UI ahead of a real gateway
`Order.paymentMethod: "cod" | "bkash" | "nagad" | "card"`, but only `"cod"` is actually processed (`LIVE_PAYMENT_METHODS` in `orderController.ts`). `views/PaymentMethodPicker.tsx` shows all four at checkout, the non-COD ones disabled with a "Coming soon" badge — so the payment options you plan to support are visible without any of them being able to silently fail. Orders page also grew a "Pay online before receiving" button on any order still `pending`/`shipped`, opening the same picker (informational only) for now.

### "Ask someone else to pay" — added a Share button next to Copy
`views/ShareLinkModal.tsx` — WhatsApp, Facebook, Telegram, X, SMS, Email, each pre-filled with a short message and the `/pay/<id>` link, opening that platform's own share screen. Also fixed: continuing from `/pay/<id>` while logged out only opened the login popup and required a second click after logging in — it now finishes automatically (adds the shared items to the now-logged-in viewer's cart and pushes to `/checkout`) the moment login succeeds.

### Dashboard sidebar: sticky on desktop, a drawer on mobile
Was two separate real bugs: the sidebar scrolled away with long pages (now `md:sticky md:top-0 md:h-screen`, only the content area scrolls), and on phones its full link list rendered as one un-wrapping horizontal row that ran off-screen. Rebuilt as a slim top bar ("Back to store" + a "Menu" button) on small screens — tapping "Menu" opens the same nav as a right-side drawer (reusing `components/ui/Modal`), closing on link-click or backdrop-click. Desktop keeps the permanent sticky sidebar, unchanged.

### Manage Orders — finished (was a placeholder)
`views/AdminOrdersView.tsx` (gated by `orders:manage`), replacing what had just been `<PlaceholderPage title="Manage Orders" />` since the RBAC work. See `docs/ARCHITECTURE.md`'s "Checkout & Orders" section for the full design; summary:
- Every order, filterable by status, searchable by id/name/phone; each row expands to the full item/address/total breakdown.
- Status moves through the pipeline (`pending → paid → shipped → delivered`, or `cancelled`) via a `<select>` per order — intentionally not a strict state machine, since real handling has exceptions.
- **"New order"**: a manual/phone-order form (`AdminOrderFormModal.tsx`) — search-and-add products with a live running total, the same Zila/Upazila/address fields as checkout, any payment method (not locked to COD, since the admin is recording how a phone customer actually paid, not charging them). Hits a new `POST /api/orders/admin` that shares its price/stock authority with the real checkout (`computeOrderTotals`, extracted from `createOrder`) but doesn't require a buyer account — `Order.user` is now optional, and manual orders carry `source: "manual"` so they're visually flagged and never show up in a customer's own "My Orders".
- **Printing**: a "Print" link per order opens `app/(protected)/admin/orders/[id]/print` — a plain packing-slip/invoice layout with a "Print" button calling `window.print()`. This is the browser's native print dialog (works with any printer already set up on the device, thermal receipt printers included) rather than a direct hardware/thermal-printer protocol integration, which would need WebUSB/WebSerial and a lot more fragility for a want that reads as "let me print an invoice."
- **Fixed along the way**: `GET /api/orders/:id` had no ownership check at all — any signed-in user could fetch any order (including someone else's shipping address) just by knowing/guessing its id. It's what the print view now uses, and it's properly checked (order's own buyer, or `orders:manage`).

### Known follow-ups (new)
- Order status is a free-form `<select>`, not a guarded state machine — nothing stops moving a `delivered` order back to `pending`, for instance. Deliberate for now (real workflows have exceptions), but worth reconsidering if mis-clicks become a problem.
- Cancelling an order still doesn't restore stock (`updateOrderStatus` just flips the status) — same gap noted in the checkout entry above, still open.
- The printable invoice doesn't show product images or a logo — text-only, matches what a small courier packing slip typically needs, but easy to extend if wanted.

### Fixed: changing an order's status crashed the row that was just updated
`updateOrderStatus` returned the freshly-updated order straight from `findByIdAndUpdate` without repopulating `items.product` — so the row that had just been changed collapsed back to raw, unpopulated product references (no `name`/`images`), and `item.product?.images[0]` (safe when `product` is null/absent, but not when it's a truthy non-object like a raw ObjectId) crashed rendering it. Fixed the query to populate the same way the list/print endpoints do, and made the image guard fully defensive (`item.product?.images?.[0]`) in both the admin and customer order views either way.

## 2026-09-14 (Visitors by social platform)
Added referral-source tracking to the analytics dashboard: `AnalyticsEvent` now also stores the browser's `document.referrer` on every event; `getAnalyticsOverview` takes each visitor session's *first* page_view referrer, classifies it via a new `utils/referrer.ts#classifyReferrer` (Facebook, Instagram, WhatsApp, Messenger, Telegram, X/Twitter, LinkedIn, YouTube, TikTok, Pinterest, or "Other websites"/"Direct / App"), and returns counts per source. `AdminAnalyticsView` shows this as a new "Visitors by source" bar list plus a "Social media visitors" stat tile (the sum of every recognized-platform bucket). Best-effort by nature — some in-app browsers (mobile Facebook/Instagram especially) blank or rewrite the referrer, so this undercounts rather than overcounts; no UTM tagging was added on top of it for this pass.

## 2026-09-15 (Analytics: date range filter)
The dashboard had no time dimension at all — every number was an unlabeled all-time total, so there was no way to tell "when" a spike happened or compare periods. Added a range selector (Today / Last 7 days / Last 30 days / All time, defaulting to 30 days) that scopes `GET /api/analytics/overview?range=...`: server-side (`utils/analyticsRange.ts#resolveAnalyticsRange`) resolves the preset into a `createdAt` cutoff applied to orders, traffic, and product-click queries. Product/user totals (stock, categories, roles, sign-in methods) deliberately stay unfiltered — they're "as of now" snapshots, not time-series data, so a range picker doesn't mean anything for them; a caption on the page says so explicitly. The response echoes back the resolved `{ preset, from, to }` so the page can show the actual date window instead of just the preset's name.

## 2026-09-15 (Social media ads: generate + post directly to Facebook, Instagram, X)
The user wants to run ads on 4 platforms eventually; for now, built the generate-and-post system for the 3 they named (Facebook, Instagram, X), taken as far as it can go without live API credentials — see `docs/ARCHITECTURE.md`'s new "Social media ads" section for the full design.

- New `ads:manage` permission (delegable to coadmins, same as every other admin section) gates `/admin/ads` → `views/AdminAdsView.tsx`.
- **"New ad"** (`AdminAdFormModal.tsx`): pick a product, an active promo code, or write something fully custom — each auto-fills a starting title/caption/link (freely editable after), and the image falls back to the source product's own photo if nothing is uploaded. Pick which of Facebook/Instagram/X to target; Instagram is blocked without an image, since Instagram has no text-only post type.
- **Publishing** hits real platform APIs, not a stub: `server/src/integrations/meta.ts` (Facebook + Instagram, both via Meta's Graph API on one Page access token) and `server/src/integrations/x.ts` (hand-written OAuth 1.0a signing for X's `POST /2/tweets`, no new dependency for one endpoint). Every attempt's real outcome — posted / failed / not connected — is written back onto the ad and shown per-platform in the list, which doubles as the "every ad generated and run" log that was asked for.
- **What's actually missing is just credentials**, by design — `server/.env.example` documents every key (`FACEBOOK_PAGE_ID`/`FACEBOOK_PAGE_ACCESS_TOKEN`/`INSTAGRAM_BUSINESS_ACCOUNT_ID`, `X_API_KEY`/`X_API_SECRET`/`X_ACCESS_TOKEN`/`X_ACCESS_SECRET`) plus `SERVER_PUBLIC_URL` (needed so Meta's servers can fetch an ad's image — must be a real public https address once deployed, not `localhost`). Until any of these are set, that platform just shows "Not connected" instead of erroring or pretending to have posted.

### Known follow-ups (new)
- **X posting costs money** — a paid API tier is required for write access as of X's current pricing. The signing code is correct and ready, but this is the one integration where "add the keys" isn't free.
- **Instagram needs a genuinely public image URL** — in local dev, `SERVER_PUBLIC_URL` defaults to `localhost`, which Meta's servers can't reach; Instagram posting can only be end-to-end tested once this is deployed somewhere with a real domain.
- X image tweets aren't supported (text + link only) — would need X's separate v1.1 multipart media-upload endpoint.
- No scheduling — "New ad" creates it as a draft (all platforms `"pending"`), and posting is a separate manual "Post to all" / per-platform click. Nothing posts automatically or at a future time.
- The 4th platform mentioned but not named yet isn't built — this covers only Facebook, Instagram, and X.

## 2026-09-15 (Delete confirmations now use an in-app modal)
Every "Delete this?" confirmation (Products, Banners, Promo Codes, Ads) used the browser's plain `window.confirm()` popup. Replaced with the app's own modal: `lib/confirm.ts#confirmDialog()` is a drop-in-ish, promise-based replacement (`if (await confirmDialog("Delete X?")) { ... }`) backed by `controllers/useConfirmStore.ts` (zustand) and rendered once globally by `views/ConfirmDialog.tsx` (mounted in the root layout). Any future destructive action can reuse it with one line instead of reaching for `window.confirm`.

## 2026-09-15 (Email verification + forgot/reset password)
Built the full system now, ahead of having a real domain or SMTP credentials — see `docs/ARCHITECTURE.md`'s new "Email verification & password reset" section. Both features share one secure pattern (`utils/authTokens.ts`): a random token is emailed, only its SHA-256 hash is ever stored, with an expiry.

- **Forgot password**: "Forgot password?" on the login form (now a third mode of the existing `AuthModal`, alongside Sign in/Sign up) → `POST /api/auth/forgot-password` (always the same generic response, so it can't be used to check who has an account) → a real match gets an emailed link to `/reset-password?token=...` (`views/ResetPasswordView.tsx`), which sets a new password and signs them straight in.
- **Email verification**: a local (email/password) signup now starts `isEmailVerified: false` and gets an emailed link to `/verify-email?token=...` (`views/VerifyEmailView.tsx`); OAuth accounts start `true` (Google/Facebook already proved the address). `views/EmailVerificationBanner.tsx` shows a reminder + "Resend email" button on every page under `(protected)` for an unverified account.
- **Deliberately not enforced yet**: verification is a reminder only — nothing (login, checkout, anything) is blocked by an unverified email. Hard-blocking anything on this would be risky before confirming real email delivery actually works, since that would lock out every customer the moment SMTP is misconfigured or still unset.
- **Nothing sends yet** — `server/.env`'s `SMTP_*` values are still blank (no domain to send credibly from). Every email call already fails silently and non-blockingly by design (same as the existing "welcome" email), so this doesn't break registration, login, or the reset request — it just means no email actually arrives until real SMTP credentials are added, at which point this starts working with no code changes.

### Known follow-ups (new)
- No rate-limiting on `/forgot-password` or `/resend-verification` — fine at current scale, worth adding if abuse ever becomes a concern.
- No decision yet on whether/where to eventually make verification a hard requirement (e.g. before checkout) — intentionally left as a reminder-only banner for now.

## 2026-09-15 (Checkout: reuse a saved delivery address, or save a new one)
`User.phone`/`deliveryLocation` already prefilled checkout (built in the "Zila/Upazila location picker" pass), but that only ever happened via a manual edit in Settings — checkout itself never wrote back to the account, and there was no explicit way to pick between the saved address and a new one (just editable prefilled fields). Finished both halves — see `docs/ARCHITECTURE.md`'s "Checkout & Orders" section.

- `CheckoutView` now shows a radio choice once `User.deliveryLocation` is fully set: "Use my saved delivery details" (phone/zila/upazila/addressLine shown disabled, straight from the account) or "Deliver to a new address" (blank, editable fields). `fullName` is always editable either way — it's the order's recipient, not necessarily re-saved to the account.
- Choosing "new address" reveals a "Save this phone number and address for next time" checkbox (checked by default, including for a first-time customer with nothing saved yet). On a successful order, if checked, it fire-and-forgets `PATCH /api/auth/profile` (phone only — deliberately doesn't touch `name`, since the checkout name field is the recipient, which may differ from the account holder) then `PATCH /api/auth/delivery-location`, updating `useAuthStore` with the result — same non-blocking convention as the rest of the app's post-success side effects, so a failed save-for-later never surfaces as a checkout error.
- `ZilaUpazilaFields` gained an optional `disabled` prop (used to render the saved-address zila/upazila selects read-only) — backward compatible, `SettingsView`'s existing usage is unaffected.
- Verified end-to-end in the running app: placed an order as a user with no saved address (checkbox pre-checked, address got saved to Settings afterward), then reloaded checkout with a fresh cart and confirmed the "Use my saved delivery details" option appeared pre-selected and correctly autofilled, with "Deliver to a new address" clearing the fields back to blank.

## 2026-09-15 (Marketing campaigns: finished — email/SMS to opted-in customers)
The backend half of this (`Campaign` model, `campaignController`/`campaignRoutes`, the `sms.ts` integration, `marketing:manage` permission, `User.marketingOptIn`, and even the admin composer `AdminCampaignFormModal`) already existed in the codebase from earlier work, but was never actually reachable: no admin page/route rendered the composer, no sidebar link pointed at it, and — the real gap — there was nowhere in the app for a customer to ever set `marketingOptIn.email`/`.sms` to true, so the audience would always have been empty. Finished wiring all of it into the running app; see the new "Marketing campaigns (email/SMS)" section in `docs/ARCHITECTURE.md` for the full design (this whole subsystem had never been documented until now).

- **Customer opt-in, the missing piece**: Settings gained a "Promotions" section (`views/SettingsView.tsx`'s new `MarketingSection`) — two checkboxes (email, SMS) using the already-existing `PATCH /api/auth/marketing-opt-in`; SMS is disabled with a hint until a phone number exists. Checkout gained a "Send me SMS about offers and promotions" checkbox (only shown if not already opted in) that saves the opt-in non-blockingly after a successful order, same fire-and-forget convention as checkout's save-delivery-address side effect (see the previous entry).
- **Admin UI, the other missing piece**: new `views/AdminCampaignsView.tsx` at `/admin/campaigns` (gated by `marketing:manage`, guarded by `useRequirePermission` like every other admin section) — lists past campaigns (title, source badge, channels, sent/failed/recipient stats, date) with a "New campaign" button opening the existing `AdminCampaignFormModal`. Added "Marketing Campaigns" to `DashboardSidebar` alongside the other permission-gated admin links.
- **Verified live**: opted a test account into both email and SMS from Settings (persisted across a reload), confirmed the checkout SMS checkbox only appears pre-opt-in, then sent a real "custom" campaign from `/admin/campaigns` — the audience preview correctly showed "1 opted in" for email and "1 opted in — SMS not connected yet" for SMS (no `SMS_API_URL`/`SMS_API_KEY` set), and the resulting campaign row correctly recorded `0 sent · 1 failed · 1 recipients` (the send attempt itself worked; delivery failed only because `server/.env`'s `SMTP_*` values are still blank, same known gap as every other transactional email in this app).

### Known follow-ups (new)
- `SMTP_*` and `SMS_API_URL`/`SMS_API_KEY`/`SMS_SENDER_ID` are all still unset in `server/.env` — campaigns are fully wired but nothing actually arrives until real credentials are added for one or both channels, no code changes needed at that point.
- No unsubscribe link in the campaign email body, and no per-recipient send log (only the aggregate `stats` on the `Campaign` document) — fine at this store's scale, worth revisiting if campaign volume grows or a compliance requirement demands it.
- No scheduling — sending is immediate on submit, same as the social-ads "publish" flow.

## 2026-09-15 (Fixed overselling race; order cancellation + stock restore; admin dashboard overview; contact-message inbox)
Wrote up a prioritized `docs/REMAINING_WORK.md` after a "what's left" discussion, then picked off four items from it in one pass — see that file for the full numbered list (items are marked ✅ there as they're finished, so it stays a live checklist rather than a stale snapshot).

- **Fixed a real overselling bug** (`orderController.ts`): `createOrder`/`adminCreateOrder` read a product's stock, then decremented it as a separate step — under concurrent checkouts for the last unit of a product, both could pass the read-time check before either decrement landed, taking stock negative. Both endpoints now run inside a `mongoose` session transaction, and the decrement itself is one atomic conditional update (`Product.updateOne({_id, stock: {$gte: quantity}}, {$inc: {stock: -quantity}})`) per line — a losing request throws a "just sold out" 409 and the whole transaction (any earlier lines in the same order included) rolls back instead of partially applying. Verified live: placed a normal order end-to-end with the new transaction path with no regression.
- **Order cancellation + stock restore**: new `PATCH /api/orders/:id/cancel` (`cancelMyOrder`) lets a customer cancel their own order while it's still `"pending"` — `views/OrdersView.tsx` gained a "Cancel order" button (behind the existing `confirmDialog`, danger style). Both this and the admin's existing `updateOrderStatus` now call a shared `restoreStock` helper whenever a status change actually lands on `"cancelled"` from something else, guarded against double-restoring a re-sent "cancelled" status. Verified live: cancelled a real order and watched the product's stock count go back up by exactly the ordered quantity.
- **Admin dashboard overview** — see `docs/ARCHITECTURE.md`'s new section. `/dashboard` now shows admins/coadmins-with-`orders:manage`-or-`products:manage` an operational overview (`views/AdminDashboardOverview.tsx`: pending orders, total orders, revenue, total products, low-stock list) instead of reusing the customer "my orders" view, via new lightweight `GET /api/orders/stats`/`GET /api/products/stats` endpoints (deliberately not folded into the heavier `analytics:manage`-gated analytics payload, so an orders- or products-only coadmin still gets a useful home screen). A new `views/DashboardHome.tsx` picks which view to render; coadmins with neither permission still see the customer view. Verified live against real data (pending/total order counts and revenue matched, low-stock list populated correctly).
- **Contact message inbox** — see `docs/ARCHITECTURE.md`'s new section. New `messages:manage` permission (delegable, same pattern as every other admin section) gates `/admin/messages` (`views/AdminMessagesView.tsx`): every submitted message, an unread indicator (`ContactMessage.isRead`, new field), expand-to-read (which marks it read), a `mailto:` "Reply by email" link, and delete. Previously a message was saved to the database and *only* readable via a best-effort email notification that doesn't arrive without real SMTP credentials (still unset) — this is the first way to actually read one without querying the database directly. Verified live: submitted a real message through `/contact` and confirmed it — plus an older pre-existing one — showed up correctly in the new inbox with working read/unread state.
- **Did not attempt**: SMS gateway configuration (item 2.6 in `REMAINING_WORK.md`) — this needs a real account with a Bangladeshi bulk-SMS provider, which isn't something achievable by writing code alone.

### Known follow-ups (new)
- Admin dashboard overview has no date-range filtering (unlike the full Analytics page) — it's deliberately "right now" numbers only, a quick-glance screen rather than a report.
- Contact inbox has no pagination — fine while message volume is low.
- Order cancellation is customer-initiated only while `"pending"`; there's still no way for a customer to request cancellation of an order already `paid`/`shipped` (would need a "request cancellation" flow reviewed by an admin, not a unilateral self-service one, if that's ever wanted).

## 2026-09-15 (Reliability & security hardening: rate limiting, helmet, tests+CI, error monitoring, process supervision)
Picked off Priority 3 of `docs/REMAINING_WORK.md` (items 3.1-3.5) in one pass — see `docs/ARCHITECTURE.md`'s new "Reliability & security hardening" section for the full design. Added dependencies: `express-rate-limit`, `helmet`, `@sentry/node`, `pm2` (server); `vitest` + `vite` (both server and frontend, dev-only).

- **Rate limiting**: new `middleware/rateLimit.ts` — `authLimiter` (20/15min per IP) on register/login/forgot-password/reset-password/resend-verification, `contactLimiter` (5/hour per IP) on the contact form.
- **Security headers**: `helmet()` added to `app.ts`, with `contentSecurityPolicy` off (this is a JSON API, not an HTML app) and `crossOriginResourcePolicy: "cross-origin"` so the frontend can still load `/uploads` images cross-origin (helmet's default would otherwise block that). Verified live: product images still render, login/dashboard/checkout all still work with the new headers in place.
- **Tests + CI**: Vitest wired into both packages (`npm test`). 28 backend tests — the highest-value ones cover `computeOrderTotals`'s exact pricing/delivery-fee/promo-discount math and the stock-race fix from the previous entry (`decrementStockAtomically`, mocked Mongoose models) — plus `analyticsRange`/`referrer`/`authTokens`/`permissions`. 15 frontend tests cover the pure `lib/` money/promo functions (`currency`, `delivery`, `promo`). New `.github/workflows/ci.yml` type-checks, lints, and runs both suites on push/PR. **This repo still has no `.git`**, so CI is real groundwork but can't actually execute until the project is initialized and pushed to GitHub — flagged clearly rather than left implicit.
- **Error monitoring**: new `utils/errorMonitoring.ts`, same zero-config-required pattern as the SMS/ads integrations — every error still logs to the console as before, and also reports to Sentry once `SENTRY_DSN` is set (still blank). Wired into the Express error handler and two new process-level handlers.
- **Process crash handling + supervision**: `server.ts` gained `uncaughtException`/`unhandledRejection` handlers that report the error and exit deliberately (Node's own guidance — the process is in an undefined state afterward) rather than one that already reaches Express's own error handler (which Express 5 auto-forwards async route-handler errors to, without crashing the process at all — this is the last-resort case). New `ecosystem.config.js` + `npm run start:pm2` gives a real, no-account-needed way to actually restart it afterward on a self-managed host.
- **Found and left as-is (documented, not silently ignored)**: `npm audit` flags a high-severity `js-yaml` advisory via `pm2`'s own bundled dependency — the suggested fix is a breaking *downgrade* to an older pm2, which doesn't actually resolve to a safer version. Low real-world risk here since `js-yaml` is only used internally by pm2 to parse its own developer-authored config, never attacker-controlled input in this app, so left as the current (latest) pm2 version rather than downgrading.

### Known follow-ups (new)
- Test coverage is a real baseline, not exhaustive — no integration test actually exercises the MongoDB transaction/session code path end-to-end (that would need something like `mongodb-memory-server`'s replica-set mode); the transaction logic itself was verified manually against the real dev database instead (see the previous entry).
- CI can't run yet — no `.git` in this repo. First real step once one exists: push and watch the workflow actually execute once.
- Frontend has no error-monitoring/Sentry equivalent yet (a render-time React error still just shows Next's default error UI) — this pass was scoped to the backend, where an actual outage matters most.
- No log aggregation/centralization — `morgan`'s request logs and `console.error` still only exist wherever the process's stdout/stderr happens to go.

### Fixed: cart-count badge hydration mismatch (pre-existing, unrelated to the pass above)
User-reported: `Uncaught Error: Hydration failed...` in the browser console, pointing at `Navbar.tsx`'s `CartButton`. Same root cause class as the earlier-documented auth-store hydration bug, different mechanism: `useCartStore` uses zustand's `persist` middleware (localStorage), which by default reads storage *synchronously* the moment the store module evaluates in the browser — so a returning visitor's very first client render already had cart items, while the server (no localStorage access) always rendered an empty cart. React then throws the mismatch and discards/regenerates that subtree.

- Fixed with `skipHydration: true` on `useCartStore`'s `persist` config, plus one explicit `useCartStore.persist.rehydrate()` call in `app/providers.tsx`'s existing mount effect (right next to `useAuthStore`'s own `hydrate()` call — same pattern, same file, same effect). The first client render now agrees with the server (both start empty); the real cart loads immediately after mount instead.
- Verified live: cleared the cart, hard-reloaded a page with items in it, confirmed no hydration error in the console and the navbar badge still correctly shows the count moments after load.
- This bug predated today's other changes — nothing in the Priority 2/3 work above touched `useCartStore.ts` or the cart badge — it just hadn't been noticed/reported until now.
- **Found and fixed the identical latent bug in `useUIStore`** while investigating: it also uses `persist` (for `locale` only, via `partialize`), and `LanguageSwitcher`'s `<select value={locale}>` would mismatch the exact same way the moment anyone actually switched to Bangla — nobody had hit it yet, but it was a real ticking time bomb, not a hypothetical. Fixed identically (`skipHydration: true` + `useUIStore.persist.rehydrate()` in the same effect). Verified live: switched to Bangla, hard-reloaded, confirmed no hydration error and the dropdown correctly settled on "bn" a moment after load.

## 2026-09-15 (Priority 4 cleanup: reviews, wishlist, categories, analytics retention, campaign unsubscribe + recipient log, Privacy/Terms)
Worked through `docs/REMAINING_WORK.md`'s Priority 4 (4.1-4.9), skipping only what's genuinely blocked or a design tradeoff rather than a quick fix — see that file for the item-by-item detail and the ✅ marks. See `docs/ARCHITECTURE.md`'s new "Reviews, wishlist, and categories" section and the updated "Marketing campaigns"/"Analytics" sections for the full design.

- **Reviews**: `isVerifiedPurchase` computed at submit time (delivered order containing this product?), shown as a badge; new `reviews:manage` permission gives inline moderation (delete) on every review. Verified live — badge appeared only once a real order was marked delivered, and delete worked with the confirm dialog.
- **Wishlist**: "Move all to cart" on the dashboard — adds every saved item to the cart, then removes it from the wishlist (a real move).
- **Categories**: `getProducts`'/`getProductCategories`'s matching and grouping are now case-insensitive — "Shoes" and "shoes" merge into one category. Verified directly against the API (`?category=CLOTHING` and `?category=clothing` returned identical counts). Deliberately did *not* build a full separate Category collection — that's a bigger, separate project from the actual reported bug (case mismatch), which is what's fixed.
- **Analytics retention**: a MongoDB TTL index now auto-deletes `AnalyticsEvent` documents after 180 days — no cron job, nothing to maintain.
- **Campaign unsubscribe + per-recipient log**: every campaign email now has a one-click, per-recipient unsubscribe footer (HMAC-signed with the existing `JWT_SECRET`, no new secret, no login required, no stored/expiring token) hitting new `GET /api/auth/unsubscribe`; `Campaign.recipients` now logs every individual send attempt, viewable by expanding a campaign row in `/admin/campaigns`. Verified live end-to-end: hand-generated a valid token, hit the endpoint, confirmed the account's email opt-in flipped off in Settings, confirmed a bad token gets a 400 — then restored the test account's opt-in back to on afterward so testing didn't leave a side effect.
- **Privacy Policy + Terms of Service**: real `/privacy` and `/terms` pages (not placeholders), linked from the footer and sitemap, written to accurately reflect what this app actually does (opt-in marketing with unsubscribe, OAuth sign-in, COD-only payment, 180-day analytics retention).
- **Own bug caught during testing**: my first pass at review deletion (`useProductReviews.ts#remove`) just decremented `total` locally instead of refetching — left a stale average-rating badge showing after the last review was deleted. Fixed to refetch the page after a successful delete, same as `submit` already does.

### Explicitly not done, with reasons (not silently skipped)
- **4.5 (social ads platform gaps)** — genuinely blocked: X needs a paid API tier, Instagram needs a real public domain to even test, and the unnamed "4th platform" has nothing concrete to build.
- **4.7 (`STORE_CITY` duplication)** — not a token problem, a structural one: a real fix needs either a monorepo/shared-package restructuring of both projects' build setups, or a runtime API dependency for a value used in synchronous contexts (Zod schemas, `sitemap.ts`). Left as-is (already documented in both places) rather than risk the build for a rarely-changed single string, pending an explicit go-ahead.
- **4.8 (protected-page session-check flash)** — not a token problem either: a real fix (Next.js Middleware checking auth before render) would redirect immediately on no session, which undoes the deliberately-shipped "open the login modal in place" behavior on Dashboard/Orders/Settings/Admin (see the 2026-09-14 "Login-in-place at checkout" entry). Flagged rather than guessed at which behavior should win.

### Known follow-ups (new)
- Review moderation has no audit trail (who deleted what, when) — just gone once removed.
- The unsubscribe link only ever turns off email marketing, never SMS — intentional (matches what the link says), but there's no equivalent one-click SMS opt-out today.
- `Campaign.recipients` will grow one entry per send per campaign — fine at this store's scale, would need revisiting (or moving to a separate collection) if campaign volume/audience size grows by orders of magnitude.

## 2026-09-15 (Analytics: admin-only, filtered "Reset analytics data")
User asked for a way to instantly zero out analytics from the dashboard (all the dev/test traffic accumulated while building shouldn't count once the store actually launches), then asked for it to be admin-only and filterable rather than a blanket wipe-everything button. See `docs/ARCHITECTURE.md`'s "Analytics" section for the full design.

- New `GET /api/analytics/events/count` and `DELETE /api/analytics/events` (both `adminOnly`, not delegable to a coadmin even one holding `analytics:manage` — permanently deleting history is a different order of consequence than viewing it). Both share one `buildEventFilter()` so the count previewed is always exactly what gets deleted. Filter has two independent dimensions: which field(s) (`page_view`/`product_click` — checkboxes, both checked by default) and an age cutoff (all time / 30d / 7d / before today).
- `views/AdminAnalyticsView.tsx` gained a "Reset analytics data" section, hidden entirely unless `user.role === "admin"`. Clicking delete previews the real count first (a 0-match filter short-circuits with a plain toast instead of a needless confirm dialog), then the app's usual danger-styled `confirmDialog` shows the actual number before anything is deleted, then reloads the dashboard so the zeroed numbers show immediately.
- Iterated once on the UI mid-build: first pass used a single "event type" dropdown (All/Page views/Product clicks); user asked for an explicit field-selection UI instead, so it's now two checkboxes — functionally the same three states, but the selection is visible rather than bundled into one dropdown option.
- Verified live end-to-end: a 0-match filter (30+ day old events, all of which were from that same day) correctly skipped the confirm dialog; a real delete (all-time, both fields) showed "278 analytics events" in the confirm dialog, and after confirming, Page views/Unique visitors/Social media visitors/Product clicks all went to 0 immediately while Products/Users/Orders/Revenue stayed untouched.

### Known follow-ups (new)
- No audit trail for a reset (who cleared what, when) — same gap as review moderation above.
- The two checkboxes map 1:1 to `AnalyticsEvent.type` — there's nothing more granular to select (e.g. "just visitors by source") since every dashboard stat derives from one of those two underlying event types.

## 2026-09-15 (Image uploads: Cloudinary-ready, credentials deliberately left blank)
User asked for a recommendation on moving off local-disk image storage (item 3.6 in `docs/REMAINING_WORK.md`) — confirmed it's the standard fix, recommended Cloudinary over raw S3/R2 for this app specifically (purpose-built for images, generous free tier, automatic optimization), then was asked to build it with the credentials deliberately left blank ("we will add it later"). See `docs/ARCHITECTURE.md`'s new "Image uploads" section for the full design.

- New `utils/cloudinary.ts` (`isCloudinaryConfigured`, `uploadBufferToCloudinary`) — same zero-config-required pattern as every other optional integration in this app (SMS, social ads, Sentry).
- `utils/upload.ts` reworked: multer now uses `memoryStorage()` instead of `diskStorage()`, and a new shared `storeUploadedFile(file)` is what every controller calls — it uploads to Cloudinary if configured, otherwise falls back to writing the buffer to `server/uploads/` exactly as before. Updated all four call sites that used to build `/uploads/${file.filename}` by hand: `productController.ts` (create/update, including the multi-file array case), `bannerController.ts` (create/update), `adController.ts` (create), `authController.ts` (profile picture).
- No frontend changes needed at all — `lib/api.ts#toUploadUrl` and `adController.ts#toPublicImageUrl` already passed an absolute URL through unchanged, so a mix of pre-Cloudinary local paths and post-Cloudinary https URLs displays/posts correctly with zero migration.
- Added `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` to both `.env.example` and the real `.env` — all left blank, exactly as asked, so uploads keep working locally with zero setup until real credentials are added.
- **Verified live**: browser-based file-input upload testing hit a tooling flakiness (the extension's file input kept showing 0 files selected despite reporting success — a testing-environment quirk, not an app bug), so verified instead via a direct authenticated `fetch()` multipart request run from the browser console using the real session token: confirmed a `PUT /api/products/:id` upload returns a fresh local `/uploads/<uuid>.jpg` path (Cloudinary correctly not configured yet) and that the file was actually written to disk with the right bytes. Cleaned up the test data (reverted the product's images back to empty, deleted the test file) afterward.

### Known follow-ups (new)
- Nothing uploaded to Cloudinary yet, obviously — waiting on real `CLOUDINARY_*` credentials from the user.
- No cleanup of the *old* local file when an image is replaced/removed (e.g. `updateProduct` swapping an image) — this was already the case before Cloudinary existed (orphaned local files), not a regression introduced here, but worth revisiting together if it matters once real storage costs are involved.

## 2026-09-15 (Rebrand: "Shoje Pai" + a generated "SP" logo)
Renamed the store from the placeholder "New Ecommerce" to "Shoje Pai" and replaced the plain "Shop." text wordmark with a real (if simple) logo mark.

- `lib/seo.ts#SITE_NAME` is the single source of truth and was already threaded through metadata, `/privacy`, `/terms`, the checkout share text, and the print view — changing it there alone updated all of those with no further edits needed.
- New `views/Logo.tsx` — a generic "SP" monogram as inline SVG (not a raster image asset, so it stays crisp at any size and follows the theme's own `fill-primary`/`fill-primary-foreground` tokens rather than a hardcoded color, same convention as everything else in this app) plus the `SITE_NAME` wordmark next to it. Takes a `wordmarkClassName` prop so it reads correctly on both a light background (`Navbar`, default `text-foreground`) and the dark footer (`text-background`).
- Replaced the hardcoded `Shop<span>.</span>` markup in both `Navbar.tsx` and `Footer.tsx` with `<Logo />`. Also caught and fixed a stray hardcoded `"New Ecommerce"` in the footer's copyright line that `SITE_NAME` didn't actually cover before this (a real gap, not just a rename) — it now reads `SITE_NAME`.
- Verified live: logo renders correctly in both the navbar and footer (including on the dark footer background), browser tab title updated to "Shoje Pai — Shop Quality Products Online", and the footer copyright now reads "© 2026 Shoje Pai."

### Known follow-ups (new)
- This is a code-drawn SVG monogram, not a designed logo — fine as a placeholder/generic mark, but a real brand identity (custom wordmark, favicon to match, social preview image) would need actual design work, not something generated here.
- The favicon (`app/favicon.ico` or equivalent) and any Open Graph preview image weren't touched — still whatever the project started with, unrelated to the in-page logo mark.

### Follow-up: wordmark styling ("looking dull")
The plain flat-color `SITE_NAME` text read as an afterthought next to the colored monogram. `Logo.tsx` now bolds it (`font-bold tracking-tight`) and splits it into two tones — every word except the last stays in the surrounding text color, the last word (`"Pai"`) renders in `text-primary` — derived from `SITE_NAME` by splitting on whitespace rather than hardcoding "Shoje"/"Pai" separately, so a future name change stays in sync automatically (a single-word name just renders whole in the accent color). Verified live in both the light navbar and dark footer.

## 2026-09-15 (Rebrand correction: "Shoje Pai" -> "Shohoje Pai")
User caught that the previous same-day rebrand used the wrong transliteration of the Bengali name. Since `SITE_NAME` in `frontend/src/lib/seo.ts` is the single source of truth (already threaded through metadata, /privacy, /terms, checkout share text, print view, Navbar/Footer via `Logo.tsx`), the fix was a one-line change there - no other files needed edits.

## 2026-09-16 to 2026-09-18 (First production deployment: GitHub, Vercel, Railway, real domain, Google OAuth live)
The site went from local-only to actually live on the internet at `shohojepai.com`. Not a code feature, but worth logging since it's the biggest state change so far and several real bugs were caught and fixed along the way.

- **Git installed and repo created** — this machine had no git at all (installed via `winget install Git.Git`). Root `.gitignore` added; first commit (242 files, verified clean — no `node_modules`/`.env`/build artifacts/`.claude/` before committing). Pushed to `github.com/shohojepaiofficial-spec/main-app`. Pushing over HTTPS needs an interactive browser login (Git Credential Manager) — doesn't work from a fully non-interactive shell, works fine from a real terminal.
- **Fixed a real dependency conflict blocking every Vercel build**: `frontend/package.json` pinned `@types/node: "^20"`, but `vitest@5.0.1` peer-requires `@types/node@^22`. Worked locally only because existing `node_modules` predated the mismatch; Vercel's clean install hit it immediately (`ERESOLVE`). Fixed by bumping to `^22`.
- **Deployed backend to Railway** (root directory `server`, Nixpacks auto-detects `npm run build`/`npm start`, `process.env.PORT` already handled correctly in `server.ts`) and **frontend to Vercel** (root directory `frontend`).
- **Connected the real domain** (`shohojepai.com`, bought + mailbox set up on Namecheap): apex + `www` on Vercel, `api.shohojepai.com` on Railway (CNAME + TXT ownership-verification record, cert auto-issued once both verified). `www` was initially left as an independent live domain instead of redirecting to the apex — caused a CORS failure (see below) until set to redirect.
- **Debugged a chain of production-only issues, one at a time, each confirmed via direct HTTP/DNS checks rather than guessing**:
  - CORS blocked by a trailing slash on Railway's `CLIENT_URL` (`https://shohojepai.vercel.app/` vs the browser's origin with no trailing slash — exact-string CORS match, so one character broke it).
  - `www.shohojepai.com` serving independently (200, no redirect) instead of redirecting to the apex — since `app.ts`'s CORS `origin` is a single static string (not real multi-origin matching), any request from `www` got back an `Access-Control-Allow-Origin` for the apex only, and browsers correctly blocked the mismatch. Fixed by setting `www` to redirect to the apex in Vercel, not by changing the CORS code.
  - A bulk/paste edit to Vercel's env vars wiped `AUTH_SECRET`/`AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` without the user noticing, causing NextAuth's generic `Configuration` error — re-added individually.
  - `POST /api/auth/oauth-sync` 403s — `INTERNAL_API_SECRET` missing/mismatched between Vercel and Railway (shared-secret header check in `middleware/internalAuth.ts`).
  - Fresh Atlas database from switching connection strings meant no admin existed; `npm run promote-admin` needs to be re-run against whichever database is actually live.
- **Google OAuth verified working end-to-end in production**: real `AUTH_GOOGLE_ID/SECRET`, redirect URI added in Google Cloud Console for the real domain, consent screen still in "Testing" (publish attempt didn't go through — user hit a wall, not yet resolved, low priority since it only limits *which* Google accounts can sign in, not whether the feature works).
- Updated `docs/REMAINING_WORK.md`: checked off 1.2 (real domain), 1.5 (admin account), 2.5 (Google sign-in) as done.

### Known follow-ups (new)
- OAuth consent screen is still in "Testing" publishing status — only accounts added as test users can sign in with Google right now. Revisit "Publish App" in Google Cloud Console when there's time to debug why it wouldn't go through.
- Facebook sign-in (`AUTH_FACEBOOK_ID/SECRET`) still blank — Google-only for now.
- The secrets that appeared in this chat session's history while debugging (`AUTH_SECRET`, `AUTH_GOOGLE_SECRET`, `INTERNAL_API_SECRET`) are good candidates to rotate later as routine hygiene, not because of any known compromise.

## 2026-09-18 (Fixed: every image upload silently failed once real Cloudinary credentials were added)
User reported a product they created had no image. Real `CLOUDINARY_CLOUD_NAME`/`API_KEY`/`API_SECRET` were added to `server/.env` since the [2026-09-15 Cloudinary-ready entry above](#2026-09-15-image-uploads-cloudinary-ready-credentials-deliberately-left-blank) (which only ever tested the credentials-blank fallback path) — this was the first upload attempt with real credentials in place, and it broke every image upload in the app (products, banners, ads, avatars), not just products, since they all go through the same `utils/cloudinary.ts`.

- **Root cause**: `utils/cloudinary.ts` called `cloudinary.config(...)` at module top level, guarded by `if (isCloudinaryConfigured())`. `tsx`/esbuild hoists a module's static imports ahead of the importing module's own statements, so when `server.ts` runs, this module's top-level code executes *before* `server.ts`'s own `dotenv.config()` call — confirmed with instrumented debug output showing `cloudinary.ts`'s top-level log firing before `server.ts` even printed its first line. `process.env.CLOUDINARY_*` was therefore still empty when `isCloudinaryConfigured()` ran, so `cloudinary.config()` was never called — and since it only ran once, at import time, later requests (env now loaded) still hit an unconfigured SDK singleton and failed with `Must supply api_key`. The local-disk fallback never had this problem since it doesn't touch the Cloudinary SDK at all — which is why this was invisible for three days of the credentials-blank fallback working fine.
- **Fix**: moved the `cloudinary.config(...)` call into a `configureCloudinary()` function called at the top of `uploadBufferToCloudinary()` on every upload, instead of once at module load. Cheap (just sets a few fields on the SDK's in-memory config), and guarantees `process.env` is read at call time — after real requests can only happen once the server has fully started, `dotenv.config()` has unconditionally already run by then.
- **Verified**: isolated call to `uploadBufferToCloudinary()` with the real `.env` credentials — failed with `Must supply api_key` before the fix, returned a real `https://res.cloudinary.com/...` URL after.

### Known follow-ups (new)
- None of the four upload call sites (products, banners, ads, avatars) were individually broken — this was a single shared-module bug, so no per-controller follow-up needed.

## 2026-09-19 (Fixed: product image selection silently dropped every file; deployed the fix + a missing next/image host)
Same investigation continued — after the Cloudinary fix above, `PUT`/`POST /api/products` kept returning 200 with `images: []` no matter what. Traced with temporary debug logging on both sides (backend `productController.ts`, frontend `ProductFormModal.tsx`, both removed before committing) plus a raw DB read confirming the saved doc really did have `images: []`.

- **Root cause**: `ProductFormModal.tsx`'s file `<input onChange>` calls `onFilesSelected(e.target.files)` then immediately does `e.target.value = ""` to reset the native input. `onFilesSelected` queued `setNewImages((prev) => [...prev, ...Array.from(files)]...)` — a functional updater, which React defers until after the event handler returns. By the time React actually ran it, `e.target.value = ""` had already cleared the *same live* `FileList` object `files` still pointed to (a `FileList` from `e.target.files` isn't a snapshot — clearing the input mutates it in place), so `Array.from(files)` always saw an empty list. `BannerFormModal`'s equivalent input never broke because it reads `e.target.files?.[0]` synchronously in the `onChange` itself, before any reset. Confirmed via console debug output: `existingImages.length` and `MAX_IMAGES` were exactly right, but `next.length` was always `0` regardless of how many files were picked.
- **Fix**: `Array.from(files)` now happens synchronously inside `onFilesSelected`, before `setNewImages` is called — the plain array is captured before the input reset can touch it.
- **Also fixed while investigating**: `next.config.ts` never had `res.cloudinary.com` in `images.remotePatterns`, so once uploads actually started reaching Cloudinary, `next/image` refused to render them (`Invalid src prop ... hostname "res.cloudinary.com" is not configured`). Added it (no path/account restriction, since the cloud name isn't secret and shouldn't be hardcoded into committed config — a different environment could use a different Cloudinary account).
- **Deployed and verified against production** (`shohojepai.com` / Railway API, same Atlas cluster as local dev): before pushing, `curl`ing `https://shohojepai.com/_next/image?url=<cloudinary-url>` returned `400` — confirming the missing `remotePatterns` entry was live-broken, not just a local gap. Product/category text itself was already rendering fine in production's SSR HTML (`getProductCategories` and per-product `category` field were never affected by either bug) — the user's "category stays empty" read turned out to be the broken image making the whole card look wrong, not a separate bug. Pushed both this fix and the 2026-09-18 Cloudinary fix together (`8d8e1be`, `cd01155`) once local testing confirmed image upload + display worked end-to-end.
- Confirmed the `utils/cloudinary.ts` module-load-timing bug from the entry above is **tsx/esbuild-specific**: built the server with `npm run build` (real `tsc`, what Railway actually runs) and inspected `dist/server.js` — `tsc`'s CommonJS output preserves statement order exactly as written, so `dotenv.config()` genuinely runs before `require("./app")` there. That fix was still correct/worth keeping (cheap, and removes a footgun for local dev), but it wasn't what was breaking production.

### Known follow-ups (new)
- **Not yet confirmed**: whether `CLOUDINARY_CLOUD_NAME`/`API_KEY`/`API_SECRET` are actually set in Railway's environment variables (as opposed to just the local `.env`). If they're missing there, the backend falls back to local-disk storage, which likely doesn't survive a Railway redeploy/restart — new production uploads would keep failing to persist even with today's fixes in place. Needs checking directly in Railway's dashboard.

## 2026-09-19 (Homepage redesign: hero banner + category tiles)
User feedback on the homepage, iterated in two rounds: the hero banner was "too big, dull old looking", and the category tiles were "too dull and generic" — a second pass on the first fix ("the banner containing black box that holding texts feels the same") made clear the issue was the *pattern* (text boxed over a photo), not just its styling.

- **`HeroSlider.tsx`**: was a near-fullscreen (`100vh`, up to 820px) image with a full dark gradient wash and text floating in a box on top. Replaced with a contained `420px`/`440px` card split into a solid-color text panel (gradient from the banner's own `accentColor`, admin-set per slide) beside the photo — not overlapping it — plus a large, low-opacity "SP" mark (the same shape as the navbar `Logo.tsx`) watermarked into the color panel for texture instead of a flat fill. Stacks (photo on top, text below) on mobile, side-by-side on `md+`.
- **`CategoryShowcase.tsx` / `/categories`**: was flat bordered boxes with just text. Extracted a shared `CategoryTile.tsx` (used by both the homepage's top-6 showcase and the full `/categories` grid, so they read as one design instead of drifting apart) — vivid two-stop gradient tiles (hashed deterministically per category name, not index, so a category keeps its color across re-sorts), a best-effort keyword-matched lucide icon (falls back to a generic tag for anything unmatched — categories are free text, see `productController.ts`), and a heavier/smoother hover (500ms ease-out lift + scale + shadow + a soft white overlay fade-in, icon rotates/scales).
- `CategoryTile.tsx` resolves its icon dynamically from a fixed set of module-level lucide components (`iconFor(category)` picks one by regex) — triggers the newer `react-hooks/static-components` lint rule if rendered as JSX (`<Icon />`), even though the set is stable and never actually creates a component at render time. Used `createElement(Icon, props)` instead, which says the same thing without the false positive.

## 2026-09-19 (Cloudinary/local-disk cleanup on delete and replace)
User noticed a real gap (already flagged as a known follow-up back in the 2026-09-15 Cloudinary entry): deleting a product/banner, or replacing one of their images, only ever removed the MongoDB reference — the actual Cloudinary asset (or local file, pre-Cloudinary) stuck around forever, quietly accumulating storage. Asked for the industry-standard approach rather than a quick patch.

- Corrected a misconception up front: Cloudinary's upload response doesn't include a reusable "delete link" — that would let anyone holding the URL delete the asset. Deletion is `cloudinary.uploader.destroy(publicId)`, signed with the server's own API secret, admin-API-only. So there was never anything to hide from the frontend; the only real question was *when* to call it.
- **`utils/cloudinary.ts`**: added `deleteFromCloudinary(publicId)` — calls `destroy`, treats both `"ok"` and `"not found"` as success (the asset being already gone is the desired end state either way).
- **`utils/upload.ts`**: added `deleteUploadedFile(url)`, the shared entry point every controller now calls. Deliberately doesn't store a separate `publicId` field alongside every image URL (which would mean migrating the existing plain-`string[]` schema on `Product`/`Banner`/`User` and touching every frontend component that reads `.images`/`.image` as a bare string) — instead parses the public_id back out of the `secure_url` we already store, since `uploadBufferToCloudinary` always uploads with the same fixed `{ folder: "new-ecommerce" }` and no transformations, so the URL shape is entirely within this app's own control. Never throws — cleanup is best-effort maintenance, logged and swallowed, never something that should fail the delete/update request that triggered it. Silently no-ops for anything that isn't our own Cloudinary folder or local `/uploads/` path (e.g. a Google/Facebook OAuth avatar URL on a user's profile).
- Wired into `productController` (`deleteProduct`, and `updateProduct` for images dropped via `existingImages`), `bannerController` (`deleteBanner`, and `updateBanner` when a new image replaces the old one), and `authController#updateProfile` (old avatar, skipped automatically for OAuth URLs per above). All three respond to the client first, then run cleanup afterward — the user shouldn't wait on non-critical housekeeping, and the DB write (the part that matters) has already succeeded either way. For updates specifically, the new image is uploaded and saved *before* the old one is deleted, never the reverse, so a failed upload or DB write can't leave a product/banner with no image at all.
- **Deliberately left `adController#deleteAd` alone**: an ad's image is often *borrowed* from its linked product (`createAd` falls back to `product.images[0]` when nothing was uploaded specifically for the ad) rather than an ad-exclusive upload, so blindly deleting it on ad deletion risks deleting an image a live product still depends on. Left a comment explaining why rather than solving it now — would need the schema to distinguish "own upload" from "borrowed reference" first.
- **Verified against real Cloudinary**, not just code review: uploaded a throwaway test image, called `deleteFromCloudinary` on it, and confirmed via `cloudinary.api.resource(publicId)` (the authoritative Admin API lookup, not a CDN-fronted `fetch` — which kept returning a stale `200` from edge cache right after deletion and would have been a false pass) that the asset was genuinely gone afterward.

### Known follow-ups (new)
- Ad image cleanup remains unimplemented for the borrowed-reference reason above.
- No cleanup runs for images that were *already* orphaned before this feature existed (any product/banner deleted prior to today). Would need a one-off script cross-referencing every stored image URL against Cloudinary's asset list, not something to run unattended.

## 2026-09-19 (Replaced the generated "SP" mark with the real logo)
User supplied a real logo (`logo.png`, downloaded locally) — a cart/checkmark/arrow icon with a "Shohoje Pai" wordmark baked into the same image. The "transparent" background in every preview was actually a checkerboard pattern rendered as opaque pixels (confirmed via `sharp`'s channel stats — alpha was `255` everywhere in the source file), not real alpha, so it had to be chroma-keyed out before the icon could be used anywhere: any near-neutral (low-saturation) pixel in the checker's lightness range (~130–240) got its alpha zeroed, which cleanly preserved the logo's true blacks and saturated greens. Verified the result had genuine variable alpha (not just visually — the "transparent" checkerboard in image previews looks identical to a real one) by checking `sharp` stats directly and by compositing onto a solid color.

- Cropped just the icon (no wordmark) out to `public/logo-icon.png`, and a pure-white-silhouette variant (`public/logo-icon-white.png`, alpha preserved, RGB forced to white) for low-opacity use on colored backgrounds.
- **`Logo.tsx`**: swapped the generated `<svg>` rounded-square "SP" mark for `next/image` pointed at `logo-icon.png`. Kept the wordmark as live text (unchanged) rather than cropping that from the source image too — it still follows the theme's color tokens and dark mode instead of becoming a fixed-color raster.
- **`HeroSlider.tsx`**: the brand watermark added earlier this session (see the "banner containing black box" redesign entry) now uses `logo-icon-white.png` instead of a hand-drawn SVG approximation of the same mark — same treatment (low opacity, rotated, corner-anchored).
- **`src/app/icon.png`**: added a square (512×512), padded, transparent-background version as a Next.js `icon` file-convention favicon — coexists with the existing `favicon.ico` (Next.js emits both `<link>` tags; browsers prefer the more specific PNG). Confirmed both render in the page `<head>` rather than one silently overriding the other.

## 2026-09-19 (Hero banner: arch divider + logo badge, styled after a reference mockup)
User pointed at a reference design dropped in `public/banner-target.webp` (a generic "Exclusive Furniture" promo template) and asked for its structure — a curved arch dividing a color panel from the product photo, a circular badge sitting on that seam, small decorative touches — adapted to this app's own branding rather than copied outright: keep the banner's own admin-picked `accentColor` as the dominant tone (not the reference's blue), build the arch from a blend of the site's own green and that accent color, put the real logo only in the seam badge (not also as a second watermark elsewhere), skip the reference's phone/address footer in favor of a short motto line, and use our own logo wherever the reference had Vercel's triangle mark.

- **The arch** (`HeroSlider.tsx`): a `rounded-r-full` div positioned with its flat left edge exactly at the panel's own right edge (`left-[38%]`, matching the panel's `md:w-[38%]`) and extending into the photo — deliberately *not* centered on the seam, so it never has to compete with the panel's text for stacking (it only ever overlaps the photo, never the panel's content). `linear-gradient(160deg, #15914f, accentColor)` — the site's own primary green blended with the banner's own color, not a copy of the reference's flat blue.
- **The logo badge**: replaces the reference's "75% DISCOUNT" circle — a `radial-gradient` in the *banner's own* `accentColor` (lighter highlight, darker rim, same glossy-shaded look the reference's badge had) with a white ring and drop shadow, straddling the seam, with `logo-icon-white.png` centered in it. This is now the *only* place the logo appears on the banner — the diffuse low-opacity watermark added earlier in the day's redesign was removed, since the user asked for the logo in the badge only, not both.
- Skipped the reference's phone-number/website-address footer row entirely; replaced with a short italic motto line ("Simple shopping, happier days.") in the same rough position.
- Left the reference's top-left logo+dots slot empty rather than duplicating the badge's logo a second time — explicitly left to judgment call, and one clear placement read better than two.
- Arch/badge are `hidden` below `md:` — both are inherently a left/right split concept that doesn't translate to the stacked mobile layout, so mobile keeps the plain flat panel gradient from the previous redesign unchanged.
- **Removed `public/vercel.svg`** (the default `create-next-app` scaffold leftover, confirmed unused anywhere in `src`) per "change the triangle vercel svg with our logo" — nothing in the codebase referenced it, so there was nothing to actually swap in-place; deleting it was the real fix. Left the other scaffold SVGs (`next.svg`, `file.svg`, `globe.svg`, `window.svg`) alone since only this one was named.
- **User's verdict on the arch/badge hero redesign above**: strongly negative ("ugliest thing i have ever seen an ai ever do"). Left as-is pending the user supplying their own replacement code for that part specifically — did not touch `HeroSlider.tsx` again in the entry below, only `CategoryTile.tsx`.

## 2026-09-19 (Category card: swapped in a user-supplied card pattern)
User pasted a specific HTML/CSS card design (a "stat card" style — colored top strip that slides down on hover to reveal a dark rounded panel underneath, a 3-dot menu, a large number stat, a small caption) and asked for the category tiles to use that pattern, following the site's own colors/content instead of the pasted example's.

- **`CategoryTile.tsx`** rewritten around the pasted structure: `grid-rows-[56px_auto]`, the colored top strip (kept this session's existing per-category hashed gradient rather than the pasted example's fixed cyan, and the lucide icon-matching from the previous redesign instead of the example's fixed unrelated SVG) slides down 4 (`group-hover:translate-y-4`) on hover, and the dark panel below it overlaps by `top-[-10px]` with `rounded-[10px]` on all corners — same layered-reveal trick as the pasted code. Recolored the dark panel to the site's own `bg-foreground`/`text-background` token pair (rather than the pasted example's arbitrary navy/lavender) — doubles as correct dark-mode support for free, since those two tokens already swap in `:root[data-theme="dark"]`.
- Content mapping: title -> category name, the big stat -> product count, the small caption -> "product(s) in stock" (the pasted example's own two stats, "32hrs" and "Last Week-36hrs", don't have an equivalent second number for categories, so the caption became a label instead of a second stat).
- Both `CategoryShowcase.tsx` (homepage) and `/categories` needed no structural changes, just dropped the now-meaningless forced `h-36`/`h-40` override on `CategoryTile` — the new layout sizes itself from its grid rows + content instead of a caller-supplied height.
- **Follow-up same day**: the dark stat panel (`bg-foreground`) read as flat black rather than on-brand, and the six per-category strip colors (orange/teal/purple/blue/red alongside green) read as an off-brand rainbow. Both switched to the site's own green family — the panel to a fixed dark-green gradient (`PANEL_GRADIENT`, `#163a26 -> #0a1f13`), the six strip gradients to different green shades/tints instead of different hues — so a category still gets its own distinct-but-related tone rather than losing all variation.
- Re-verified the `icon.png` favicon from the earlier logo-replacement entry is still correctly wired (both `<link rel="icon">` tags present, `/icon.png` returns 200) after the user reported still seeing the old tab icon — almost certainly browser-side favicon caching (notoriously sticky across reloads) rather than a regression, since nothing server-side changed.
- **Second follow-up same day**: even the dark-green `PANEL_GRADIENT` still read as too close to black. Replaced it with an actual glassmorphic treatment instead of a third attempt at "a dark solid color that isn't black" — `rgba()` green (translucent, not opaque) + `backdrop-blur-md` + a thin `border-white/25` edge, so it reads as green glass over the page rather than another flat-colored block.

## 2026-09-19 (Hero banner rebuilt from a user-supplied reference, verbatim structure)
The arch/badge redesign two entries above landed badly ("ugliest thing i have ever seen an ai ever do"). User then supplied a complete, self-contained HTML/CSS reference banner (a furniture-sale template: white card, one big glowing circle arch, a discount badge on its peak, script+bold two-tier headline, decorative dots, a footer row) and asked for `HeroSlider.tsx` to match it structurally, swapping in real content and this app's own admin-configurable `accentColor` for the reference's fixed palette, and the offer badge showing the banner's actual linked promo instead of a hardcoded "75%".

- **Root layout** (`layout.tsx`): added `Playfair Display` (italic-only, via `next/font/google`, `--font-playfair`) for the headline's small script accent line — the reference's exact font pairing (Poppins-equivalent body + Playfair italic accent), not something to approximate with a generic serif fallback.
- **The arch, precisely this time**: a single SVG `<circle cx="614" cy="292.5" r="255">` in a `viewBox="0 0 1024 585"` (`preserveAspectRatio="none"`, matched by the container's own `aspect-[1024/585]`) — the *same* technique as the reference, not my own approximation of it. The circle's right half is simply painted over by the photo layer drawn after it (z-10 over z-0), exactly like the reference's `<rect>`-after-`<circle>` trick, rather than a CSS clip-path. Gradient is vertical (pale accent -> vivid accent -> pale accent, top to bottom) using this banner's own `accentColor`, not the reference's fixed green — `color-mix()` derives the pale tips and a separate darkened shade (`darkAccent`) that stands in for the reference's fixed `--dark` on every solid UI element (badge, button, footer icon, stub line, side line).
- **Badge**: same position/size/white-ring treatment as the reference's discount circle, but shows `{promo.value}% OFF` / `{formatCurrency(promo.value)} OFF` when the banner has a linked promo, falling back to the white logo mark when it doesn't (a banner isn't required to carry a discount).
- **Base is white again**: the two entries above put text on a solid/glass *colored* panel — this reference (and the very first `banner-target.webp` mockup, in hindsight) both use a white/light card with color confined to the arch and solid accent elements, which is a real structural difference from what shipped earlier, not just a color swap. Headline/description now use the site's own `--foreground`/`--muted` tokens (reliable contrast on white, doesn't swing with `accentColor`); only the badge/button/icon/line backgrounds use the per-banner `darkAccent`.
- Decorative dots and the side accent line carried over from the reference at the same percentage coordinates (same 1024x585 basis as the arch), `hidden sm:block` — too fussy to read at very small sizes.
- Slider controls (prev/next/dot-indicators) moved from per-slide (duplicated per slide, only the active one mattering) to rendered once outside the slide map — they're identical regardless of which slide is active, and the photo they sit over is always the same rightmost ~40% region across slides.
- **Verified**: three consecutive fetches of `/` all `200` (no flakiness this time), gradient `<stop>` colors confirmed rendering with each slide's real `accentColor` (not a static value), and both slides' actual titles/motto text present in the output.

### Same-day follow-up: shorter banner, exact 50/50 split, arch simplified to a thin colored divider
User feedback: banner still too tall, wanted the two halves exactly 50/50 (were content 56% / photo ~40%, arch offset at the seam rather than centered), the arch itself in the plain selected `accentColor` rather than a pale-to-vivid gradient blend, the color's presence extended beyond just the arch/badge ("the whole banner should contain the flavour of the color"), and the arch reframed as a thin ~25px divider rather than the reference's huge 255px-radius circle.

- Height: `aspect-[1024/585]` (~1.75:1) -> `aspect-[1024/380]` (~2.7:1) — meaningfully shorter at the same container width.
- Split: content `w-1/2` and photo `left: 50%` (was `w-[56%]` / `left: 60%`) — exact halves, both sides now meeting at the same seam the divider sits on.
- Arch: swapped the SVG circle + gradient entirely for a thin (`16px` mobile / `20px` sm / `25px` md+) `rounded-full` bar centered exactly on the 50% seam, filled with `slide.accentColor` as-is — no blending, matching "the selected color from the dashboard" literally rather than a derived tint.
- Added `cardTint` (`color-mix(in srgb, accentColor, white 93%)`) as the per-slide card background, replacing flat white — extends the accent color's presence across the whole card (decorative dots and the footer divider now also derive their color from it) rather than confining it to the arch/badge/button.
- Scaled down font-size `clamp()` values and internal spacing (margins/gaps/padding) roughly proportionally to the height reduction — clamp() tracks viewport width, not container height, so shrinking the aspect ratio without also shrinking these would have caused text overflow in the now-shorter card.
- Badge shrunk `14%` -> `12%` of card width and its ring color switched from a fixed white to the new `cardTint` (so it blends with the tinted card instead of standing out starkly against a background that's no longer pure white).

### Same-day follow-up: three bugs from the shorter card
- **Text overflowing up into the navbar**: the text column is a `flex flex-col` with `h-full` inside the now-much-shorter card, but flex items don't shrink below their own content's min-content size by default — on narrow viewports the column's real content (logo, headline, description, CTAs, footer) needed more vertical room than the shortened `aspect-[1024/380]` card actually had, so it overflowed past the card's bounds. Fixed with a `min-h-[280px]` floor on the card (so it stops shrinking below a usable minimum regardless of aspect-ratio math) plus `min-h-0` on the text column (lets it actually participate in flex shrinking instead of forcing overflow).
- **Left ("previous") nav button invisible**: it sat at `left-3` of the whole card, which is now the light-tinted text-column side (added in the "flavour of the color" pass) — a white/25 translucent button all but disappeared against a near-white background. Moved both prev/next buttons together, centered under the photo half (`left: 75%`) where they always have photo contrast behind them, regardless of which half used to host them.
- **Badge content**: was conditionally offer-text-or-logo; now always just the logo (its one placement on the banner, no exceptions) — the offer moved into the text column instead, as a small pill left-aligned with the rest of the copy (eyebrow/title/description), directly above the CTA row, only rendered when the banner actually has a linked promo.

### Same-day follow-up: banner content rendering over the fixed navbar, plus three smaller requests
- **Root cause of the navbar overlap**: `Navbar.tsx` is `fixed top-0 z-30`. The hero's own internal elements used `z-30` (text column) and `z-40` (nav buttons/dot indicators) — both compare directly against the navbar's `z-30` in whatever stacking context is nearest common ancestor, so wherever the hero's content visually reached the navbar's screen region, the `z-40` elements legitimately rendered *above* the fixed navbar. Fixed properly rather than just renumbering: added `isolate` to the hero card's outer container, which creates its own stacking context boundary — every z-index used inside (0 through 40) is now scoped *within* the card and can never escape above an external fixed-position sibling like the navbar, regardless of the actual numbers used inside.
- Added a hover lift to the whole banner card (`hover:-translate-y-1` + a deeper shadow) — it was the one card-like element on the homepage with no hover feedback at all, inconsistent with `ProductCard`/`CategoryTile`.
- Removed the small icon-only `<Logo>` that sat top-left of the text column — the badge on the arch is the logo's one placement on this banner; having it twice was left over from an earlier pass, not something asked for this round.
- Divider gradient: was a flat `slide.accentColor` fill; now `linear-gradient(to left, ...)` across the bar's own ~25px width (a lighter tint of the accent color on the photo-facing edge fading into the pure accent color on the text-facing edge) rather than the previous top-to-bottom treatment — gives the thin bar a rounded/glossy look instead of reading as a flat rectangle.

### Same-day follow-up: divider readability, and a real hover concept instead of a whole-card lift
User called out that the divider's white-lightened gradient read as washed out and the bar itself was too fat, clarified they never asked for the whole card to lift on hover, and described what hover *should* do: text in focus at rest, buttons/photo pulling focus on hover instead, plus a smaller badge logo and vertically-centered ("middle left") text.

- Divider: `16–25px` -> `8–12px`, and the gradient no longer lightens toward white (`color-mix(..., white 35%)`) — now stays within the accent color's own richness, pure color on the photo-facing edge fading to a *darkened* shade on the text-facing edge.
- Removed the whole-card `hover:-translate-y-1`/shadow-boost from the previous entry entirely — replaced with a deliberate focus-shift interaction instead: the eyebrow/title/description/offer-pill block and the footer motto both dim to 70% opacity on hover (text recedes), while the photo zooms in slightly (`scale-105`) and the CTA buttons scale up — buttons and photo are the two things hover is supposed to draw the eye to, not a generic "the whole thing perks up" lift. The decorative dots get a small rightward drift on hover too, per the user's own suggestion.
- Badge logo: `h-[38%]` of a `w-[12%]` badge -> `h-[32%]` of a `w-[9%]` badge — smaller, as asked.
- Text column: `justify-between` (spread to the top/bottom of the card) -> `justify-center` (vertically centered as a group, still left-aligned within its own half) — "the text should align in the middle left".

### Same-day follow-up: footer pinned back to the bottom, shadow toned down
The previous `justify-center` change centered *everything*, including the footer motto row — user wanted just the headline/CTA block centered, with the footer's thin-line row staying pinned to the bottom like before.

- Restructured the text column into two pieces: a `flex-1 justify-center` wrapper around the headline block + CTA row (centers within the space *above* the footer, not the full column), and the footer as a separate sibling after it — since the wrapper claims all available space via `flex-1`, the footer naturally lands at the very bottom regardless of how the block above it centers.
- Card shadow: `0 35px 70px rgba(15,40,30,0.35)` -> `0 10px 25px rgba(15,40,30,0.15)` — read as too elevated compared to every other card on the site (`ProductCard`/`CategoryTile` use much subtler shadows).

### Same-day follow-up: two-color system — a fixed "blackish" base + the dynamic accent
User's framing: the banner should use two colors, "one is selected and the other is dynamic" — a constant dark tone across every banner, paired with each banner's own admin-picked `accentColor` doing the accenting. Previously *everything* (card background included, via `cardTint`) derived from `accentColor`, so the whole card's mood swung with whatever color was picked rather than the color reading as an accent against something fixed.

- Added `CARD_BASE = "#15181c"` — a fixed dark tone, not derived from any banner's `accentColor`, now the per-slide background (was `cardTint`, a light accent-tinted wash).
- Flipped every text color from dark-on-light to light-on-dark to match: eyebrow/subtitle/footer text -> `white/70`, `white/60`, `white/80`; title and the secondary CTA -> plain `white`.
- Footer's top divider line: was accent-color-derived (`color-mix(..., transparent 75%)`); now a neutral `border-white/15`, since it's a plain structural divider, not one of the banner's accent elements.
- Badge's ring color: was `cardTint`; now `CARD_BASE` — blends seamlessly into the card on the text-column side, reads as a subtle dark separation ring on the photo side.
- Decorative dots: bumped from `transparent 55%` to `transparent 35%` (more opaque) — a semi-transparent accent-colored ring needs more presence against a genuinely dark background than it did against the previous light tint.
- The divider bar, badge fill, buttons, and offer pill all still derive from `accentColor` unchanged — they're the "dynamic" half of the two-color system; only the base flipped.

## 2026-09-19 (Hero banner rebuilt again — "the design just got destroyed" — from a second, more complete reference)
The two-color blackish/accent pass above landed badly. User supplied a second, considerably more polished reference (a "Sale Hero Banner": ambient glow, angled-cut photo panel, a pulsing CTA-icon ring, Fraunces italic eyebrow, gradient badge/icon accents) and asked to follow it directly rather than iterate further on the previous structure.

- **Font**: swapped `Playfair Display` for `Fraunces` (italic, weight 500 — `next/font/google`, `--font-fraunces`) to match this reference's actual type pairing, same scoped-to-just-the-eyebrow-line approach as before.
- **Two-color system, rebuilt around this reference's palette**: fixed `INK` (`#12141a`) card background + `CREAM`/`MUTED` fixed text tones (all constants, not from `accentColor`), paired with a dynamic two-stop `grad` gradient built from each banner's own `accentColor` (pure -> lightened via `color-mix`) standing in for the reference's fixed coral-to-amber `--grad` — badge, footer icon, and the CTA-icon circle all use it.
- **Ambient glow**: a blurred, low-opacity radial gradient in the top-left, using `accentColor` instead of the reference's fixed coral — was entirely absent from every earlier pass, adds real atmosphere.
- **Angled photo panel**: `clip-path: polygon(9% 0, 100% 0, 100% 100%, 0% 100%)` on the photo layer — a diagonal cut instead of a straight vertical seam. The reference used this same clip-path on a decorative CSS-illustrated sunset (gradients + clip-path "hills"); swapped that illustration for the banner's real photo since this app needs actual uploaded images, not decoration.
- **Pulsing CTA icon**: added `@keyframes hero-pulse-ring` to `globals.css` (a continuous ambient scale+fade loop, not hover-triggered — Tailwind has no built-in for a custom keyframe like this) behind a smaller solid icon circle. The reference's icon was a generic bag/tag SVG; put the white logo mark there instead, continuing "the logo's one spot on this banner" from recent rounds rather than reverting to no logo at all.
- **CTA button**: solid-filled -> outlined/ghost (`border-white/30`, transparent background) matching the reference's `.visit-btn` treatment, a real style change from every previous pass.
- **Split ratio**: `56%` content / `44%` photo (this reference's own numbers), superseding an earlier explicit "must be 50/50" request — given precise code to follow this time, treated the numbers in it as authoritative over a prior verbal spec.
- Kept from earlier passes (not asked to change, still applicable): the `min-h-[280px]` floor + `isolate` (navbar-overlap fix), the hover focus-shift concept (text dims, photo zooms, buttons/CTA-group scale up on hover), and the offer as a pill in the text column rather than replacing the badge's logo.

## 2026-09-19 (Category card: fixed a genuinely broken hover, coordinated the two-layer colors)
User: "the behind panel of each card get behind when hovered" + wanted the panel "half shown" at rest with hover revealing "the whole thing", plus "the color doesn't feel good".

- **Root cause of the hover bug**: the colored strip's hover state was `group-hover:translate-y-4` — sliding it *down*, further over the dark panel's own grid row, not away from it. The panel only ever appeared on top in the 10px negative-margin overlap zone (later DOM element painting above an earlier one there, independent of any transform) — so the "reveal" comment on that code was simply wrong; sliding the strip down could only ever cover more of the panel, never less. That's exactly "gets behind when hovered" — the strip crept toward the panel's own text.
- **Redesigned as two absolute-positioned layers instead of a grid**: a back layer (the dark panel, `absolute inset-0`, *always* the full card, holding all the text) and a front layer (the colored strip + icon, `absolute inset-x-0 top-0 h-1/2`, covering only the top half at rest). At rest this reads as "half shown" panel exactly as asked; on hover the front layer does `-translate-y-full` — lifts completely off the top of the card like a curtain, revealing the full back layer underneath, rather than creeping further onto it.
- **Color coordination**: replaced the single fixed `PANEL_GRADIENT` (one dark green, paired with six *differently*-hued strip gradients) with `TILE_THEMES` — six `{strip, panel}` pairs, each panel a darker variant of its own strip's exact hue, indexed by the same per-category hash so a card's two layers are always color-coordinated instead of an arbitrary fixed color sitting under six different tile colors.
- Dropped the glassmorphic treatment (`backdrop-blur-md`, translucent `rgba()` panel, `border-white/25`) from two entries ago — solid color now that the panel is a full, permanent layer rather than a thin translucent sliver; the blur read as part of "the color doesn't feel good" alongside the hue mismatch.

### Same-day: confirmed category count semantics, fixed stale banner image guidance
- Confirmed (no code change needed) that `getProductCategories`'s `count: { $sum: 1 }` is exactly "how many distinct Product documents exist in that category" — not stock quantity, not variants — matching what the user expected the card's number to mean.
- **`BannerFormModal.tsx`'s image hint was stale**: said "wide landscape image (1600×900 or larger)", left over from before this session's several hero redesigns. The current `HeroSlider.tsx` photo panel is `44%` of the banner's width at the banner's full height (`aspect-[1200/460]`) — computed out, that's `528:460`, roughly **6:5, nearly square** — not remotely a 16:9 landscape. Updated the hint to the real ratio (`~1200×1000`) and explained why, so an admin uploading a wide landscape photo doesn't get a much tighter crop than they expected.

## 2026-09-19 (Category cards: genuinely dynamic icon matching + procedural color)
User asked how the category icon selection worked (a fixed ~11-pattern keyword list — explained honestly that it wasn't hardcoded per category but was a hardcoded *pattern list*) and what it'd take to make it properly dynamic. Presented three options (broader fuzzy matching / LLM-classification cached per category / admin-assigned via a real Category model) with tradeoffs; asked to do the fuzzy-matching option plus make the color generation dynamic too, "modern, not too dull".

- **Tried fuzzy-matching the category name against the full `lucide-react` icon set first** (`icons` export, ~1800 names) via `fuse.js` — and rejected it after directly testing: at a loose threshold it confidently returns nonsense for entirely plausible categories (`"Pets"` -> a person icon, `"Cars"` -> `CanSoda`, `"Furniture"` -> `BoneFracture`, `"Candles"` -> `ChartCandlestick`), because fuzzy matching only measures character similarity, not meaning, and a huge candidate pool means more coincidental near-misses. Landed on a **curated ~90-icon candidate pool** instead (vetted, unambiguous, shopping-relevant icons only) — same technique, much safer, verified against a wide battery of real-sounding category names (including the app's own live test categories: `clothing`, `food`, `gym`, `shoes`, `sports` all resolve correctly; `perfume`/`torch`/`truck` — until `Truck` was added to the pool — safely fall back to a generic tag rather than guessing wrong).
- **Two-tier lookup** in `CategoryTile.tsx`: a `SYNONYM_HINTS` regex list (expanded from the original 11 to ~20) still goes first, for the common storefront words whose natural icon isn't lexically close to the word itself (fuzzy matching alone can't bridge `"clothing"` -> `Shirt`, they share no characters) — the fuzzy search over the curated pool is the fallback for anything the hints don't cover, not the primary mechanism.
- Confirmed this all runs in a **Server Component** (`CategoryTile.tsx` has no `"use client"`) — importing the full lucide `icons` map and `fuse.js` costs nothing in the client bundle; the fuzzy search runs at request time on the server, and only the handful of resolved SVGs actually used ever reach the browser.
- **Color**: replaced the fixed 6-pair `TILE_THEMES` array with `themeFor()` — hashes the category name into a hue (`90-165°`, kept inside the site's green family) and generates the strip/panel gradient procedurally via `hsl()`, so every category gets its own distinct color instead of cycling through 6 repeating options. Kept saturation/lightness high (`~70%`/`~55%` for the strip) per "modern, not too dull" rather than the muted tones from two entries ago.
- New dependency: `fuse.js` (added to `frontend/package.json`).

## 2026-09-19 (Category card: simplified to a single-gradient card from a user-supplied reference image)
User supplied `card.png` — a much simpler design than the curtain-reveal two-layer card: one flat gradient card, subtle concentric rings behind a large bottom-right icon, bold category name + a lighter subtitle line top-left. Rebuilt `CategoryTile.tsx` around it, keeping the icon-matching system from the previous entry unchanged (only the strip/panel layout and two-tone theme were replaced).

- `themeFor()` simplified from a `{strip, panel}` pair to one `{background, glow}` — a single diagonal gradient for the whole card (still procedurally hued per category, same green-family range) plus a lighter tint of the same hue for the decorative rings.
- Dropped the front/back layered-curtain structure entirely — no more hover-reveal mechanic to get wrong. Icon now renders large (72px, thin stroke) bottom-right, bleeding slightly off the corner like the reference's bag illustration, with a small scale/lift on hover instead.
- Subtitle line changed from `"{count} product(s) in stock"` to `"{count} product(s)"` — closer to the reference's short second line ("Trendy" under "Shopping").

## 2026-09-20 (SMTP email finished — the last blank field, verified live)
Returned to `docs/REMAINING_WORK.md`'s item 1.1. `server/.env` already had `SMTP_HOST` (`mail.privateemail.com`, Namecheap Private Email), `SMTP_PORT` (465), `SMTP_USER`/`SMTP_FROM` (`hello@shohojepai.com`) filled in from the earlier domain/mailbox setup — only `SMTP_PASS` was ever blank. User added it directly to `.env` themselves rather than pasting it into chat.

- Confirmed the code side (`utils/sendEmail.ts` + every call site — `authController.ts`'s verify-email/welcome/password-reset, `contactController.ts`, `campaignController.ts`) needed zero changes — all already fire-and-forget with `.catch(err => console.error(...))`, exactly the "never let a down SMTP server break the request that triggered it" pattern from when this was built.
- **Verified with a real send**, not just checking the env var was non-empty: a temporary script called `sendEmail()` directly with the real `.env` loaded, sending to the mailbox's own address (`hello@shohojepai.com`) — got back a genuine success from the SMTP server, confirming the password is actually correct and the connection/auth work end-to-end, not just that a value exists. Removed the script after.
- Updated `docs/REMAINING_WORK.md`: moved 1.1 from "Still open" to "Already finished", and corrected a stale line under 3.6 that still said Cloudinary credentials were "deliberately left blank" — they were added and fixed on 2026-09-18/19 (see those dates' entries above) but the tracking doc was never updated to reflect it.

### Same-day follow-up: production emails still weren't arriving — found a real bug along the way
User re-tested through the actual app (not just the isolated script) — submitted real contact-form messages through both the live production API (`api.shohojepai.com`) and local, confirmed via a direct request that `api.shohojepai.com` really is the Railway-hosted server specifically (so this genuinely isolated "does Railway's own SMTP config work" from local). Both requests returned `201`, but no email arrived at either. User then pasted Railway's actual server log, which showed `ValidationError: ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` on every request.

- **Root cause**: `app.ts` never set `trust proxy`. Railway sits the app behind a reverse proxy that adds `X-Forwarded-For`; without `trust proxy` configured, Express doesn't trust that header, and `express-rate-limit`'s own safety check (guarding against a real IP-spoofing risk) throws on every request through a rate-limited route (the contact form, every auth endpoint) — confirmed live in Railway's own logs, not just a local guess.
- **Investigated but did not confirm** a specific causal chain to the missing emails (initially suspected this became an unhandled promise rejection that crashed the process mid-send, killing the fire-and-forget email call) — traced `express-rate-limit`'s source directly and found the error is properly caught by its own `handleAsyncErrors` wrapper and forwarded to Express's `next(error)`, not a raw unhandled rejection, so that specific theory doesn't hold up. Said so plainly rather than asserting a mechanism that wasn't actually verified.
- **Fixed regardless**: added `app.set("trust proxy", 1)` (trusts exactly one hop, matching Railway's real topology — not `true`, which would trust an arbitrary chain a client could forge). Correct either way, since it also means rate limiting wasn't keying requests by the real per-client IP before this.
- **Verified after deploying the fix**: pushed, waited for Railway to redeploy, submitted another real contact-form test to production with a timestamped subject line — user confirmed the email arrived this time.

## 2026-09-20 (Real contact info: item 1.4)
`frontend/src/lib/contact.ts` (the single source every contact-info display reads from — Navbar, Footer, `WhatsAppButton`, `/contact`) had placeholder phone/address; `CONTACT_EMAIL` there and `server/.env`'s `CONTACT_EMAIL` were already real (`hello@shohojepai.com`, set during the SMTP/domain work), so only phone and address needed filling in.

- `CONTACT_PHONE_DISPLAY` -> `+880 1700-502013`, `CONTACT_PHONE_TEL` -> `+8801700502013` (the `tel:` link format), `WHATSAPP_NUMBER` -> `8801700502013` (digits only, no `+`, what `wa.me` links expect), `CONTACT_ADDRESS` -> `55/56 Niloy, Chouhatta, Sylhet`.
- Grepped the repo for the old placeholder strings (`000-0000`, `123 Market St`, the placeholder digit-only number) to confirm nothing else hardcoded them outside this one file — clean.
- Updated `docs/REMAINING_WORK.md`: 1.4 moved from "Still open" to "Already finished".

## 2026-09-20 (bKash integration — real payment gateway, item 1.3; Facebook login removed)
User's decision on 1.3: launch on Cash on Delivery + bKash only, drop Nagad/Card entirely rather than keep them as "coming soon" placeholders, and build the real bKash integration now rather than just trimming the checkout UI. See `docs/ARCHITECTURE.md`'s "Checkout & Orders" (new bKash bullets) and "Auth" sections for the full design.

- **Verified the actual API shape live against bKash's sandbox before writing any code**, since bKash's own docs are internally inconsistent — its "Request URL" headings for Execute/Query Payment describe `paymentID` as a URL path segment, but hitting that literally returns a misleading AWS API Gateway `403 IncompleteSignatureException` (not a real bKash error). Grant Token, Create Payment, Execute Payment (`paymentID` in a JSON body to a fixed path, not the URL), and Query Payment were each called for real against `tokenized.sandbox.bka.sh` via curl first, confirming the correct request/response shape and, critically, that a `callbackURL` with our own full path (not a bare domain) comes back byte-for-byte with `?paymentID=&status=&signature=` appended rather than rewritten to some other path.
- **`server/src/integrations/bkash.ts`** (new) — `isBkashConfigured`, `createBkashPayment`, `executeBkashPayment`, `queryBkashPayment`. Grant Token is re-requested (not refreshed) whenever the in-memory cached one nears its ~1-hour expiry — bKash's Refresh Token API is rate-limited to twice an hour, re-granting isn't, so this sidesteps that limit rather than tracking it.
- **`orderController.ts#createOrder`**: for `paymentMethod: "bkash"`, the order is created and stock decremented exactly like COD (same transaction, same `computeOrderTotals`/`decrementStockAtomically`), then — *after* the transaction commits, since an external API call should never happen inside one — `createBkashPayment` starts a payment session for that order's total and the response includes `bkashRedirectUrl` for the frontend to send the browser to. If starting the bKash session fails, the order is cancelled and its stock restored immediately (`restoreStock`, the same helper cancellation already used) rather than left as an unpayable "pending" order.
- **New public `GET /api/orders/bkash/callback`** (`bkashCallback`) — where bKash redirects the customer's own browser after paying (or cancelling, or failing). Looks the order up by the `bkashPaymentID` stored on it at creation (not by trusting anything else in the URL). A non-success status cancels the order + restores stock, same as any other cancellation. A success status calls Execute Payment, falling back to Query Payment if Execute doesn't come back `"Completed"` (covers a repeat hit of the same callback landing on bKash's one-execution-per-paymentID limit instead of a real result) — then redirects on to the frontend's own `${CLIENT_URL}/checkout/bkash-result?status=...&order=...`.
- **Frontend**: `CheckoutView.tsx` redirects straight to `bkashRedirectUrl` for a bKash order instead of clearing the cart / showing "order placed" — payment isn't confirmed yet. New `views/BkashResultView.tsx` + `app/(protected)/checkout/bkash-result/page.tsx` is what the callback lands on: only clears the cart once `status=success` actually comes back, leaves it untouched on failure/cancellation so there's still something to retry checkout with (nothing was charged either way, and the order was already cancelled server-side).
- **`PaymentMethod` trimmed from `"cod" | "bkash" | "nagad" | "card"` to `"cod" | "bkash"`** everywhere (Order model + schema enum, frontend model, `PaymentMethodPicker.tsx`, checkout/admin-order-form zod schemas, admin payment-label maps) — Nagad and Card are gone outright, not just hidden, per the "COD + bKash only" decision.
- **Credentials**: `server/.env`'s `BKASH_*` values are bKash's own published **sandbox** test credentials (freely handed out at developer.bka.sh for anyone to test against, not secret) — test money only, nothing real is charged. `.env.example` documents where to get real merchant credentials; swapping in the five `BKASH_*` values (plus `BKASH_BASE_URL`) is the only change needed to go live, no code changes.
- **Not yet done**: an actual end-to-end browser test (real wallet number + OTP + PIN on bKash's sandbox payment page) — verified every API call in isolation instead, since that's the part achievable without a browser session. Worth a real click-through test before relying on this in production.

### Facebook login removed
Asked separately in the same request — Facebook OAuth sign-in never had real credentials wired up in production (`AUTH_FACEBOOK_ID`/`SECRET` were always blank, Google-only was already the real state — see the 2026-09-16 to 18 deployment entry), so this was pure cleanup, not a migration: removed the `Facebook` provider from `frontend/src/lib/auth.ts`, the "Continue with Facebook" button from `AuthModal.tsx`, `"facebook"` from `AuthProvider` in both the frontend model and `server/src/models/User.ts` (type + schema enum), simplified `SettingsView.tsx`'s provider check to Google-only, and removed the always-blank `AUTH_FACEBOOK_ID`/`SECRET` lines from `frontend/.env.local`. Facebook *ads* posting (`integrations/meta.ts`, `/admin/ads`), the Facebook share option on "ask someone else to pay" links, and the Facebook social-media icon in the Navbar/Footer are unrelated features and were left untouched. Also updated `/privacy` and `/terms` (Google-only sign-in language, bKash added as a payment-processing data-sharing party).
- Both changes verified with a full type-check (`tsc --noEmit`, both packages, clean) and the existing test suites (28 backend + 15 frontend, all passing).

## 2026-09-20 (Fixed: verification/reset emails bouncing as spam)
User tried to verify a real account and got a bounce back from the recipient's mail server (`prod-lbout-phx.jellyfish.systems`, one of Namecheap's outbound relays): `550 Your message consists mainly of URLs which, indicative of spam.` Reproduced the likely cause by reading the actual template rather than guessing.

- **Root cause**: `sendVerificationEmail`/`forgotPassword` in `authController.ts` built their HTML as `<a href="${link}">${link}</a>` — the token-bearing URL rendered as *both* the link's href and its visible text, inside a two-sentence email with almost no other prose. Relative to the message's total length, that's mostly URL — exactly what the bounce reason describes — and `sendEmail.ts` sent HTML-only with no plain-text part at all, another common spam signal.
- **Fixed**: rewrote both templates with real explanatory prose, a proper styled button whose visible text is a description ("Confirm my email address" / "Choose a new password") rather than the raw link, and the same link given once more as a fallback plain-text URL for clients that don't render HTML buttons — so the link still appears in the body (expected, not itself the problem) without being duplicated. `sendEmail.ts` gained an optional `text` field (nodemailer's plain-text MIME part); both templates now pass one. Also improved the welcome email's subject/body to name the actual store instead of "our store", and gave it a `text` part too, while it was already being touched.
- **Verified**: a temporary script sent the new verification template through the real SMTP connection — accepted by our own relay without error. Whether *this specific* recipient's filter now accepts it can only be confirmed by the user re-testing (the original bounce was from the recipient's own mail provider's heuristics, not something we can trigger/observe from here), but this addresses the exact stated reason for the bounce.

### Also noted, not a bug
- The `ECONNREFUSED ::1:5000` error and the Fraunces Google Fonts warning the user also reported were both transient: the backend dev server had been stopped (from the bKash testing session's cleanup) and is running again now; the font warning is `next dev` failing to reach `fonts.googleapis.com` (offline/proxy at that moment) and falling back automatically — cosmetic, not a real bug.
- The `/admin/orders` "Failed to load orders" error surfaced during the previous entry's live testing was also not a bug: that particular test account genuinely has `role: "user"` / no permissions in the local dev database, so the server's `GET /api/orders` correctly 403s for it — the sidebar showing admin links anyway was a stale cached session (`useRefreshUser` only re-syncs once per page load; that one sync had raced a backend restart and failed silently). Not touched, since it wasn't the error being asked about here.

### Same-day follow-up: "Resend email" hung for 2 minutes then 500'd, in production only
User pushed the fix above, then hit a genuinely new bug re-testing on the live site: clicking "Resend email" hung, then `POST /api/auth/resend-verification` came back `500` after `120586ms` — almost exactly nodemailer's default 2-minute connection timeout — logged server-side as `Verification email failed: Connection timeout`.

- **Root cause, found by reading the code rather than guessing at infrastructure**: every other email send in this app is fire-and-forget (`sendVerificationEmail(user).catch(...)`, no `await`) specifically so a slow/unreachable SMTP server can't block or fail the request that triggered it — `register`'s own call to this exact function follows that pattern one function up. `resendVerificationEmail` alone `await`ed it directly and 500'd on failure, which is what turned an otherwise-invisible, log-only SMTP hiccup into a 2-minute hang + visible error for the user. This wasn't a Railway-specific or new problem — SMTP itself was already confirmed working from Railway (see the earlier trust-proxy entry); this endpoint was just the one place a transient failure could actually surface.
- **Fixed**: `resendVerificationEmail` now fires the email and returns immediately, matching `register`. Also gave `sendEmail.ts`'s transporter explicit `connectionTimeout`/`greetingTimeout` of 10 seconds (nodemailer's defaults are 2 minutes / 30 seconds) — reasonable for something every caller already treats as fire-and-forget, so a real hiccup logs and gives up quickly instead of holding a connection open for two minutes first.
- Verified with `tsc --noEmit` (clean) and the full test suite (28 passing) — didn't attempt to reproduce the original timeout locally, since local SMTP connectivity was never the problem.

### Same-day follow-up: the hang was fixed, but two real deliverability problems remained
User re-tested on the live site: `resend-verification` now returns `200` in ~270ms (the hang is gone), but the server log still showed `Verification email failed: Connection timeout` for that same attempt, and separately reported that any verification email that *does* arrive always lands in spam.

- **Spam-folder root cause, found via direct DNS lookup rather than guessing** — queried `shohojepai.com`'s TXT/MX records directly: SPF is correctly set up (`v=spf1 include:spf.privateemail.com ~all`), but **no DKIM record exists at all** (`default._domainkey.shohojepai.com` doesn't resolve), and the existing DMARC record's `rua` reporting address points at `dmarc.brevo.com` — a different email provider than the one actually in use (Namecheap Private Email), suggesting DNS was partly set up for Brevo at some point and never finished for Private Email. Missing DKIM on a domain that already has *some* email-auth records set up is a strong spam signal to Gmail/Outlook. **Not a code fix** — needs DKIM enabled in Namecheap's Private Email control panel and the resulting TXT record added to DNS; flagged for the user rather than guessed at, since it's their DNS panel.
- **Intermittent "Connection timeout"**: reconsidered the 10s `connectionTimeout`/`greetingTimeout` added in the previous entry — with every call site now fire-and-forget, there's no user-facing cost to being more patient, and 10s risked cutting off a real (if occasionally slow) handshake to a small mail host. Raised both to 20s, and added one retry (fresh transporter, 2s delay) before giving up and logging — turns a real fraction of transient connection hiccups into a successful send instead of a silently dropped email, at the cost of a few extra background seconds nobody is waiting on.
- Verified: `tsc --noEmit` clean, full test suite passing, and a real local send still completes on the first attempt (retry path only exercises on an actual failure, not on every call).


